---
phase: 01-librarian-inducer-mvp
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - BreakoutStrategy/feature_library/sample_renderer.py
  - BreakoutStrategy/feature_library/preprocess.py
  - BreakoutStrategy/feature_library/tests/test_sample_renderer.py
autonomous: true
requirements: [REQ-glm4v-batch-describe]
requirements_addressed: [REQ-glm4v-batch-describe]
must_haves:
  truths:
    - "chart.png 仅渲染 df_window.iloc[left_index : bo_index + 1] 区间(用户挑的左端到 BO 的闭区间),不再整段 ~200 bar 渲染。"
    - "chart.png 价格面板与成交量面板都不含 pk / bo 的 axvline 虚线;价格面板不含 legend。"
    - "chart.png 标题恒为 'Breakout sample (anonymized)',不含语义后缀(如 'right edge = BO')。"
    - "Y 轴 pivot 改用 BO close = 0%(原先是 pk close);Y 轴 ylabel 为 'Price (% from BO close)'。"
    - "字号通过 `with mpl.rc_context({'font.size': 8})` 局部覆盖,不污染全局 rcParams;title 10、ylabel/xlabel 8、tick 7,且这三个具体字号值在 figure 内确实生效。"
    - "render_sample_chart 与 _build_figure_for_inspection 形参 pk_index 重命名为 left_index(语义为'用户挑的窗口左端,兼任 consolidation anchor');preprocess.py 调用处同步从 pk_index= 改为 left_index=。"
  artifacts:
    - path: "BreakoutStrategy/feature_library/sample_renderer.py"
      provides: "脱敏渲染 + 收窄窗口 + BO close pivot + 字号压缩"
      contains: "left_index, pivot_close = float(df_window.iloc[bo_index][\"close\"]), Price (% from BO close), mpl.rc_context"
    - path: "BreakoutStrategy/feature_library/preprocess.py"
      provides: "render_sample_chart 调用处 kwarg 同步:pk_index= → left_index="
      contains: "left_index=pk_index"
    - path: "BreakoutStrategy/feature_library/tests/test_sample_renderer.py"
      provides: "扩展断言:窗口收窄、无 axvline、无 legend、BO close pivot、ylabel 文本、具体字号值生效、不污染 rcParams"
      contains: "Price (% from BO close), rc_context, axvline, get_fontsize"
  key_links:
    - from: "sample_renderer._build_figure_for_inspection"
      to: "df_window.iloc[bo_index][\"close\"]"
      via: "pivot_close 计算式"
      pattern: "df_window\\.iloc\\[bo_index\\]\\[\"close\"\\]"
    - from: "_build_figure_for_inspection"
      to: "matplotlib.rc_context"
      via: "with-block 包裹整个 figure 构造"
      pattern: "with mpl\\.rc_context"
    - from: "preprocess.preprocess_sample"
      to: "sample_renderer.render_sample_chart(left_index=pk_index)"
      via: "kwarg 同步重命名"
      pattern: "left_index=pk_index"
---

<objective>
重写 `BreakoutStrategy/feature_library/sample_renderer.py`,实现 D-05..D-07 + D-09:
- D-05 渲染窗口收窄到 `df_window.iloc[left_index : bo_index + 1]`(即用户挑的左端到 BO 的闭区间);
- D-06 视觉脱敏(删除两个面板的 pk/bo axvline + 删 legend + 标题不带语义后缀);
- D-07 Y 轴 pivot 改用 BO close(`pivot_close = float(df_window.iloc[bo_index]["close"])`),ylabel 改为 `"Price (% from BO close)"`;
- D-09 字号压缩,使用 `with mpl.rc_context({"font.size": 8})` 局部覆盖以不污染全局 rcParams。

同步把调用方 `BreakoutStrategy/feature_library/preprocess.py` 的 `render_sample_chart(pk_index=...)` 改为 `render_sample_chart(left_index=...)`。

同步扩展 `test_sample_renderer.py` 的断言覆盖以上四点(包括三个具体字号 10/8/7 的 artist-level 断言)。

Purpose: 让 chart.png 对 GLM-4V 更友好(更聚焦的画面 + 跨样本同构的零点),同时与文本通道(plan 04)共享 BO close 锚点。

Output: sample_renderer.py 重写完毕,preprocess.py kwarg 同步,新旧测试全部 PASS。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-librarian-inducer-mvp/01-CONTEXT.md
@docs/research/feature_mining_chart_pivot_decision.md
@CLAUDE.md

<interfaces>
<!-- 当前 sample_renderer.py 函数签名 -->
From BreakoutStrategy/feature_library/sample_renderer.py:
```python
def _build_figure_for_inspection(
    df_window: pd.DataFrame, bo_index: int, pk_index: int,
) -> Figure: ...

def render_sample_chart(
    sample_id: str, df_window: pd.DataFrame, bo_index: int, pk_index: int,
) -> Path: ...
```

<!-- 调用方(必须保持向后兼容 OR 同步改) -->
preprocess.py:50-55 调用:
```python
chart_path = render_sample_chart(
    sample_id=sample_id, df_window=df_window,
    bo_index=bo_index, pk_index=pk_index,
)
```

<!-- 决策细节 D-05/D-09 -->
- 形参名: 用户提示"避免完全 anchor-free 的名字以免破坏 consolidation_fields.py 字段命名一致性"
- → 选用 `left_index` (兼任"用户挑的窗口左端 ≡ consolidation anchor")
- 调用方 preprocess_sample 内部的 `pk_index=local_pk` 调用需要同步改为 `left_index=local_pk`
  (sample_picker_handler.py 的 local_pk 变量名保持不变 — 它在数据层仍是 anchor 候选)
- 渲染窗口 = df_window.iloc[left_index : bo_index + 1](收窄到挑选区间;原 df_window 仍传整段 200 bar,但渲染时切片)
- consolidation_fields.compute_consolidation_fields 仍接收原 df_window 与 left_index 计算 anchor 字段(本 plan 不改 consolidation 计算)

<!-- D-09 字号方案 -->
matplotlib OO API 不污染全局:
```python
import matplotlib as mpl
with mpl.rc_context({"font.size": 8}):
    fig = Figure(...)
    # ... figure 构造的所有代码必须在此 with 块内
ax_price.set_title("Breakout sample (anonymized)", fontsize=10)
ax_price.set_xlabel(..., fontsize=8); ax_price.set_ylabel(..., fontsize=8)
ax_price.tick_params(labelsize=7); ax_vol.tick_params(labelsize=7)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 重写 sample_renderer.py 实现 D-05..D-07 + D-09 + 同步 preprocess.py kwarg</name>
  <files>BreakoutStrategy/feature_library/sample_renderer.py, BreakoutStrategy/feature_library/preprocess.py</files>
  <read_first>
    - BreakoutStrategy/feature_library/sample_renderer.py(完整当前实现)
    - BreakoutStrategy/feature_library/preprocess.py(50-55 行调用 render_sample_chart 的位置)
    - BreakoutStrategy/UI/charts/components/candlestick.py 的 CandlestickComponent.draw 签名(本 plan 不改它)
    - .planning/phases/01-librarian-inducer-mvp/01-CONTEXT.md(D-05..D-09 决策原文,严格 verbatim)
    - docs/research/feature_mining_chart_pivot_decision.md(BO close pivot 的研究依据)
  </read_first>
  <behavior>
    - 形参 pk_index → left_index(在 _build_figure_for_inspection 与 render_sample_chart);保留 bo_index 不动。
    - 渲染窗口收窄: 在 _build_figure_for_inspection 内对 df_window 做切片 `df_window.iloc[left_index : bo_index + 1]`,后续所有绘制(CandlestickComponent.draw、volume bar、Y 轴 formatter、xlim)都基于这个收窄后的子 df。
    - 删除 ax_price.axvline (pk 橙色虚线) 与 ax_price.axvline (bo 蓝色虚线);删除 ax_vol.axvline(同两条);删除 ax_price.legend(loc="upper left")。
    - 标题保持 "Breakout sample (anonymized)" 不变(无任何语义后缀)。
    - pivot_close 改为 `float(df_window.iloc[bo_index]["close"])`(注意:这里的 df_window 是切片之前的原 df,bo_index 也是原 df 中的局部 index — 但因为切片右端是 bo_index + 1,在切片后的子 df 中 BO 的位置是 sub_df.iloc[-1])。
      为避免索引混淆,实现时:
      - 先在切片之前计算 `pivot_close = float(df_window.iloc[bo_index]["close"])`
      - 然后做 `sub_df = df_window.iloc[left_index : bo_index + 1].copy()`
      - 后续 CandlestickComponent.draw、volume bar、xlim 都用 sub_df
    - ax_price.set_ylabel("Price (% from BO close)") (D-07 verbatim 字符串)
    - _pct_fmt 内部计算公式不变,但用新 pivot_close;BO 当日价格显示 +0.0% 或 0.0%(切片右端就是 BO bar)
    - 字号通过 `with mpl.rc_context({"font.size": 8}):` 包裹 Figure 构造与所有 axes 操作。
      在 with 块内额外显式设置:
      - ax_price.set_title(..., fontsize=10)
      - ax_*.set_xlabel(..., fontsize=8); ax_*.set_ylabel(..., fontsize=8)
      - ax_*.tick_params(labelsize=7)
    - figsize=(12, 8) 与 CHART_DPI=100 不变。
    - 调用方 preprocess.py 同步: render_sample_chart 的参数名 pk_index → left_index(单行变量名替换,变量值 local_pk 不动)。
  </behavior>
  <action>
    完整重写 BreakoutStrategy/feature_library/sample_renderer.py:

    1. 模块 docstring 更新:
       - 把"标注 bo / pk 位置"改为"按 [left_index, bo_index] 收窄窗口,不含 pk/bo 虚线"
       - 把"相对 pk_close 的百分比"改为"相对 BO close 的百分比"
       - 保留 OO API 设计与 视觉脱敏(归一化方案 B)段落

    2. 顶部 import 加 `import matplotlib as mpl`(用于 mpl.rc_context)。

    3. _build_figure_for_inspection 签名:
       ```python
       def _build_figure_for_inspection(
           df_window: pd.DataFrame, bo_index: int, left_index: int,
       ) -> Figure:
       ```
       docstring 重写:
       ```
       构造脱敏 K 线图 Figure(内部工具,单元测试用)。

       渲染窗口仅含 [left_index : bo_index + 1] 闭区间(用户挑的左端到 BO),
       不再画 pk/bo 虚线,不显示 legend,标题恒为 "Breakout sample (anonymized)"。
       Y 轴 pivot = df_window.iloc[bo_index]['close'](BO close = 0%)。

       Args:
           df_window: 完整 OHLCV DataFrame(local index 体系)
           bo_index:  突破日 index(df_window 局部)
           left_index: 用户挑选的左端 index(df_window 局部, < bo_index;
                       兼任 consolidation anchor)

       Returns:
           渲染完成的 matplotlib Figure(未落盘)
       ```

    4. 函数体:
       ```python
       pivot_close = float(df_window.iloc[bo_index]["close"])
       sub_df = df_window.iloc[left_index : bo_index + 1]

       with mpl.rc_context({"font.size": 8}):
           fig = Figure(figsize=CHART_FIGSIZE, dpi=CHART_DPI)
           FigureCanvasAgg(fig)

           gs = fig.add_gridspec(
               2, 1, height_ratios=[PRICE_PANEL_RATIO, VOLUME_PANEL_RATIO],
               hspace=0.05,
           )
           ax_price = fig.add_subplot(gs[0])
           ax_vol = fig.add_subplot(gs[1], sharex=ax_price)

           # 主图:K 线(只画收窄后的子 df,无 pk/bo 虚线)
           CandlestickComponent.draw(ax_price, sub_df)

           # 脱敏标题(无语义后缀)
           ax_price.set_title("Breakout sample (anonymized)", fontsize=10)
           ax_price.set_ylabel("Price (% from BO close)", fontsize=8)
           # 不设 legend(D-06)

           # Y 轴用 BO close 作 pivot
           def _pct_fmt(price: float, _pos: int) -> str:
               if pivot_close <= 0:
                   return f"{price:.2f}"
               pct = (price - pivot_close) / pivot_close * 100
               sign = "+" if pct > 0 else ""
               return f"{sign}{pct:.1f}%"
           ax_price.yaxis.set_major_formatter(FuncFormatter(_pct_fmt))

           # 副图:volume bar(同样只画 sub_df,无 pk/bo 虚线)
           ax_vol.bar(
               range(len(sub_df)), sub_df["volume"], color="#888888", width=0.8,
           )
           ax_vol.set_ylabel("Volume", fontsize=8)
           ax_vol.set_xlabel("Bar Index", fontsize=8)
           ax_vol.grid(True, alpha=0.3)

           # 字号细化(D-09)
           ax_price.tick_params(labelsize=7)
           ax_vol.tick_params(labelsize=7)

       return fig
       ```

       注意:
       - 完全移除 ax_price.axvline / ax_vol.axvline(原 62-69 行 + 92-93 行的 4 处 axvline 调用)
       - 完全移除 ax_price.legend(loc="upper left")(原 74 行)
       - 不要遗留 pk_index 变量名

    5. render_sample_chart 签名同步:
       ```python
       def render_sample_chart(
           sample_id: str,
           df_window: pd.DataFrame,
           bo_index: int,
           left_index: int,
       ) -> Path:
       ```
       docstring args 段把 `pk_index` 改为 `left_index`,语义注释为
       "用户挑的窗口左端 index(df_window 局部, < bo_index;兼任 consolidation anchor)"。
       函数体内部调 _build_figure_for_inspection 时使用 left_index=left_index。

    6. 同步修改 BreakoutStrategy/feature_library/preprocess.py:
       原(50-55 行):
       ```python
       chart_path = render_sample_chart(
           sample_id=sample_id,
           df_window=df_window,
           bo_index=bo_index,
           pk_index=pk_index,
       )
       ```
       改为:
       ```python
       chart_path = render_sample_chart(
           sample_id=sample_id,
           df_window=df_window,
           bo_index=bo_index,
           left_index=pk_index,   # pk_index 在数据层仍是 consolidation anchor;
                                  # sample_renderer 视角是"窗口左端"
       )
       ```
       注意:preprocess_sample 函数自身的 pk_index 形参不动(它仍是 consolidation_fields.compute_consolidation_fields 的 anchor 输入)。
       sample_meta.build_meta 与 compute_consolidation_fields 调用也不动。

    7. 实现完成后人工 grep 验证:
       - `grep -c "Price (% from BO close)" BreakoutStrategy/feature_library/sample_renderer.py` == 1
       - `grep "pivot_close = float(df_window.iloc\\[bo_index\\]" BreakoutStrategy/feature_library/sample_renderer.py` 命中
       - `grep "axvline" BreakoutStrategy/feature_library/sample_renderer.py` 应为空(虚线全删)
       - `grep "ax_price.legend" BreakoutStrategy/feature_library/sample_renderer.py` 应为空
       - `grep "with mpl.rc_context" BreakoutStrategy/feature_library/sample_renderer.py` 命中
       - `grep "left_index" BreakoutStrategy/feature_library/sample_renderer.py` 至少 4 次(签名 2 次 + body 至少 2 次)
       - `grep "pk_index" BreakoutStrategy/feature_library/sample_renderer.py` 应为空(完全移除)
       - `grep "left_index=pk_index" BreakoutStrategy/feature_library/preprocess.py` 命中(BLOCKER 1 acceptance)
  </action>
  <verify>
    <automated>uv run python -c "import inspect; from BreakoutStrategy.feature_library.sample_renderer import render_sample_chart, _build_figure_for_inspection; sig1 = inspect.signature(render_sample_chart); sig2 = inspect.signature(_build_figure_for_inspection); assert 'left_index' in sig1.parameters and 'pk_index' not in sig1.parameters, sig1.parameters; assert 'left_index' in sig2.parameters and 'pk_index' not in sig2.parameters, sig2.parameters; src = inspect.getsource(_build_figure_for_inspection); assert 'axvline' not in src, 'axvline must be removed (D-06)'; assert 'Price (% from BO close)' in src, 'ylabel D-07'; assert 'mpl.rc_context' in src, 'rc_context D-09'; assert 'df_window.iloc[bo_index]' in src, 'BO close pivot D-07'; pp = open('BreakoutStrategy/feature_library/preprocess.py').read(); assert 'left_index=pk_index' in pp, 'preprocess.py kwarg must be renamed (BLOCKER 1)'"</automated>
  </verify>
  <done>
    - sample_renderer.py 与 preprocess.py 调用同步(BLOCKER 1)
    - 形参 pk_index → left_index 完全替换
    - axvline / legend 全删
    - BO close pivot + ylabel + rc_context 全部就位
  </done>
  <acceptance_criteria>
    - `grep "left_index=pk_index" BreakoutStrategy/feature_library/preprocess.py` 命中(BLOCKER 1)
    - frontmatter `files_modified` 含 preprocess.py(BLOCKER 1)
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 扩展 test_sample_renderer.py 断言新行为(含具体字号 artist 级断言)</name>
  <files>BreakoutStrategy/feature_library/tests/test_sample_renderer.py</files>
  <read_first>
    - BreakoutStrategy/feature_library/tests/test_sample_renderer.py(当前测试,补充而非重写)
    - BreakoutStrategy/feature_library/sample_renderer.py(Task 1 完成后版本)
    - .planning/phases/01-librarian-inducer-mvp/01-CONTEXT.md(D-05..D-09 行为契约)
  </read_first>
  <behavior>
    扩展现有测试 + 新增以下用例:
    - test_render_creates_png_file / test_render_idempotent: 把 `pk_index=60` 改为 `left_index=60`(API 同步)
    - test_render_sample_chart_does_not_pollute_global_backend: 把 `pk_index=1` 改为 `left_index=1`
    - test_render_sample_chart_anonymizes_title_and_normalizes_yaxis: 改 API 调用 + 加新断言:
      - ylabel 文本 == "Price (% from BO close)"
      - title == "Breakout sample (anonymized)"(无语义后缀)
      - pivot 应为 BO close (df.iloc[bo_index]["close"]),不是 pk close
    - 新增 test_render_window_is_narrowed_to_left_to_bo_inclusive: 通过 _build_figure_for_inspection 拿 ax_price,断言 ax_price.lines 或 patches 数量(K 线根数 = bo_index - left_index + 1)
    - 新增 test_render_does_not_draw_pk_or_bo_axvline: 断言 ax_price.lines 与 ax_vol.lines 中没有 axvline 风格的线(线段端点 x 相同 = vertical line),也可通过 ax_price.get_legend() is None 验证 legend 已删
    - 新增 test_render_uses_bo_close_as_pivot: BO bar 的 close 通过 _pct_fmt 应渲染为 "+0.0%" 或 "0.0%"
    - 新增 test_render_does_not_pollute_rcparams: 在 rc_context 之后断言 mpl.rcParams["font.size"] 与 with-block 之前一致(已在 does_not_pollute_global_backend 中部分覆盖,加显式 font.size 校验)
    - **新增 test_render_applies_specific_fontsizes (WARNING 6)**:在 figure 内部 artist 级别断言三个具体字号:
      - title artist 的 `get_fontsize() == 10`
      - ax_price.xaxis.label / ax_price.yaxis.label 的 `get_fontsize() == 8`
      - ax_vol.xaxis.label / ax_vol.yaxis.label 的 `get_fontsize() == 8`
      - tick labels 字号 == 7(`ax_price.get_xticklabels()[0].get_fontsize()`,`ax_vol.get_xticklabels()[0].get_fontsize()`)
  </behavior>
  <action>
    在 BreakoutStrategy/feature_library/tests/test_sample_renderer.py 中:

    1. 全文 sed-style 替换 `pk_index=` → `left_index=`(影响 4 处现有测试调用)。
       注意 fixture / 测试参数定义不需要改变量名 — 仅 API 调用关键字参数名要改。

    2. 修改 test_render_sample_chart_anonymizes_title_and_normalizes_yaxis:
       在 ax_price 取得后追加:
       ```python
       # D-07: ylabel 必须是 BO close 文本
       assert ax_price.get_ylabel() == "Price (% from BO close)", \
           f"ylabel 应为 'Price (% from BO close)',实际: {ax_price.get_ylabel()!r}"
       # D-06: 标题恒为通用文本
       assert title == "Breakout sample (anonymized)", \
           f"标题应为 'Breakout sample (anonymized)',实际: {title!r}"
       # D-07: pivot 是 BO close (= 12.8) 不是原来的 pk close (= 10.8)
       formatter = ax_price.yaxis.get_major_formatter()
       at_bo = formatter(12.8, 0)
       assert "0" in at_bo and ("+0" in at_bo or at_bo.startswith("0")), \
           f"BO close 应渲染为 0%,实际: {at_bo!r}"
       # 反向:用旧 pivot 10.8 触发的 0% 现在应是负值(因为 10.8 < 12.8)
       at_old_pk = formatter(10.8, 0)
       assert "-" in at_old_pk, \
           f"原 pk close (10.8) 在新 BO pivot 下应为负百分比,实际: {at_old_pk!r}"
       ```

    3. 新增测试函数:

       ```python
       def test_render_window_is_narrowed_to_left_to_bo_inclusive():
           """D-05: 渲染只覆盖 [left_index : bo_index + 1] 区间,不画 left 之前的 K 线。"""
           import pandas as pd
           import numpy as np
           from BreakoutStrategy.feature_library.sample_renderer import (
               _build_figure_for_inspection,
           )
           n = 50
           df = pd.DataFrame({
               "open": np.linspace(10, 20, n), "high": np.linspace(11, 21, n),
               "low": np.linspace(9, 19, n), "close": np.linspace(10, 20, n),
               "volume": np.full(n, 1e6),
           }, index=pd.date_range("2024-01-01", periods=n, freq="B"))
           fig = _build_figure_for_inspection(df, bo_index=40, left_index=20)
           ax_vol = fig.axes[1]
           # volume bar 数量 == 21 (= 40 - 20 + 1)
           bar_patches = [p for p in ax_vol.patches]
           assert len(bar_patches) == 21, \
               f"volume bar 数量应为 21,实际: {len(bar_patches)}"

       def test_render_does_not_draw_pk_or_bo_axvline():
           """D-06: 删除 pk/bo 虚线 + legend。"""
           import pandas as pd
           import numpy as np
           from BreakoutStrategy.feature_library.sample_renderer import (
               _build_figure_for_inspection,
           )
           n = 30
           df = pd.DataFrame({
               "open": np.linspace(10, 20, n), "high": np.linspace(11, 21, n),
               "low": np.linspace(9, 19, n), "close": np.linspace(10, 20, n),
               "volume": np.full(n, 1e6),
           }, index=pd.date_range("2024-01-01", periods=n, freq="B"))
           fig = _build_figure_for_inspection(df, bo_index=20, left_index=5)
           ax_price, ax_vol = fig.axes
           # 不应有 legend
           assert ax_price.get_legend() is None, "ax_price.legend 应被删除 (D-06)"
           # 检查 axvline:axvline 在 lines 中表现为 xdata 两个值相同
           def has_vline(ax):
               for line in ax.get_lines():
                   xs = line.get_xdata()
                   if len(xs) == 2 and xs[0] == xs[1]:
                       return True
               return False
           assert not has_vline(ax_price), "ax_price 不应含 axvline (pk/bo)"
           assert not has_vline(ax_vol), "ax_vol 不应含 axvline (pk/bo)"

       def test_render_does_not_pollute_global_rcparams_font_size():
           """D-09: rc_context 局部覆盖,不污染全局 font.size。"""
           import matplotlib as mpl
           from BreakoutStrategy.feature_library.sample_renderer import (
               _build_figure_for_inspection,
           )
           import pandas as pd
           import numpy as np
           font_before = mpl.rcParams["font.size"]
           n = 10
           df = pd.DataFrame({
               "open": np.linspace(10, 20, n), "high": np.linspace(11, 21, n),
               "low": np.linspace(9, 19, n), "close": np.linspace(10, 20, n),
               "volume": np.full(n, 1e6),
           }, index=pd.date_range("2024-01-01", periods=n, freq="B"))
           _build_figure_for_inspection(df, bo_index=8, left_index=2)
           assert mpl.rcParams["font.size"] == font_before, \
               f"font.size 被污染: {font_before} → {mpl.rcParams['font.size']}"

       def test_render_applies_specific_fontsizes():
           """D-09 / WARNING 6: title=10 / xlabel-ylabel=8 / ticks=7 三个具体字号在 figure 内确实生效。"""
           import pandas as pd
           import numpy as np
           from BreakoutStrategy.feature_library.sample_renderer import (
               _build_figure_for_inspection,
           )
           n = 30
           df = pd.DataFrame({
               "open": np.linspace(10, 20, n), "high": np.linspace(11, 21, n),
               "low": np.linspace(9, 19, n), "close": np.linspace(10, 20, n),
               "volume": np.full(n, 1e6),
           }, index=pd.date_range("2024-01-01", periods=n, freq="B"))
           fig = _build_figure_for_inspection(df, bo_index=20, left_index=5)
           ax_price, ax_vol = fig.axes

           # title artist == 10
           title_artist = ax_price.title
           assert title_artist.get_fontsize() == 10, \
               f"title fontsize 应为 10,实际: {title_artist.get_fontsize()}"

           # xlabel / ylabel == 8(price 子图无 xlabel,只断言 ylabel;volume 子图两者都断言)
           assert ax_price.yaxis.label.get_fontsize() == 8, \
               f"ax_price.ylabel fontsize 应为 8,实际: {ax_price.yaxis.label.get_fontsize()}"
           assert ax_vol.xaxis.label.get_fontsize() == 8, \
               f"ax_vol.xlabel fontsize 应为 8,实际: {ax_vol.xaxis.label.get_fontsize()}"
           assert ax_vol.yaxis.label.get_fontsize() == 8, \
               f"ax_vol.ylabel fontsize 应为 8,实际: {ax_vol.yaxis.label.get_fontsize()}"

           # tick labels == 7(price + volume 两个子图)
           # 注:tick_params(labelsize=7) 对所有 tick label 生效;取首个验证
           price_xticks = ax_price.get_xticklabels()
           price_yticks = ax_price.get_yticklabels()
           vol_xticks = ax_vol.get_xticklabels()
           vol_yticks = ax_vol.get_yticklabels()
           assert price_xticks and price_xticks[0].get_fontsize() == 7, \
               f"ax_price.xtick fontsize 应为 7,实际: {price_xticks[0].get_fontsize() if price_xticks else 'no ticks'}"
           assert price_yticks and price_yticks[0].get_fontsize() == 7, \
               f"ax_price.ytick fontsize 应为 7,实际: {price_yticks[0].get_fontsize() if price_yticks else 'no ticks'}"
           assert vol_xticks and vol_xticks[0].get_fontsize() == 7, \
               f"ax_vol.xtick fontsize 应为 7,实际: {vol_xticks[0].get_fontsize() if vol_xticks else 'no ticks'}"
           assert vol_yticks and vol_yticks[0].get_fontsize() == 7, \
               f"ax_vol.ytick fontsize 应为 7,实际: {vol_yticks[0].get_fontsize() if vol_yticks else 'no ticks'}"
       ```

    4. 注释中文,断言消息保持中文/英文混合(沿用 CLAUDE.md 风格)。
  </action>
  <verify>
    <automated>uv run pytest BreakoutStrategy/feature_library/tests/test_sample_renderer.py -v 2>&amp;1 | tail -40</automated>
  </verify>
  <done>
    - 所有原测试 PASS(API 同步成 left_index)
    - 5 个新测试函数全部 PASS(窗口收窄、无 axvline、BO close pivot、rcParams 不污染、具体字号 10/8/7 artist 断言)
    - test_sample_renderer.py 100% PASS
  </done>
  <acceptance_criteria>
    - `pytest -k "test_render_applies_specific_fontsizes"` PASS(WARNING 6)
    - title artist `get_fontsize() == 10` 断言通过
    - xlabel / ylabel artist `get_fontsize() == 8` 断言通过(price ylabel + vol xlabel/ylabel)
    - tick labels `get_fontsize() == 7` 断言通过(price + vol 两子图)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sample_renderer.py ↔ matplotlib | 渲染输入 df_window 来自本地 yfinance / Pickle,非 untrusted |

## STRIDE Threat Register

无外部 surface — STRIDE 不适用。本 plan 仅修改 library 渲染层,
df_window / bo_index / left_index 全部来自本地数据流。
mitigations: N/A(数据由开发者本地控制)。
</threat_model>

<verification>
- chart.png 渲染窗口收窄、视觉脱敏、BO close pivot、字号压缩四项全部生效
- 不污染全局 matplotlib backend / rcParams
- 调用方 preprocess.py 同步,sample_picker_handler.py 不动
</verification>

<success_criteria>
- `uv run pytest BreakoutStrategy/feature_library/tests/test_sample_renderer.py -v` 100% PASS
- `grep -c "axvline" BreakoutStrategy/feature_library/sample_renderer.py` 返回 0
- `grep -c "Price (% from BO close)" BreakoutStrategy/feature_library/sample_renderer.py` 返回 1
- `grep -c "with mpl.rc_context" BreakoutStrategy/feature_library/sample_renderer.py` ≥ 1
- `grep -c "pk_index" BreakoutStrategy/feature_library/sample_renderer.py` 返回 0
- `grep "df_window.iloc\\[bo_index\\]\\[\"close\"\\]" BreakoutStrategy/feature_library/sample_renderer.py` 命中
- `grep "left_index=pk_index" BreakoutStrategy/feature_library/preprocess.py` 命中(BLOCKER 1)
- `pytest -k "test_render_applies_specific_fontsizes"` PASS(WARNING 6)
</success_criteria>

<output>
After completion, create `.planning/phases/01-librarian-inducer-mvp/01-03-SUMMARY.md`
</output>
