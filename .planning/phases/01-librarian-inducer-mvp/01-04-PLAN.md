---
phase: 01-librarian-inducer-mvp
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - BreakoutStrategy/feature_library/prompts.py
  - BreakoutStrategy/feature_library/inducer_prompts.py
  - BreakoutStrategy/feature_library/tests/test_prompts.py
  - BreakoutStrategy/feature_library/tests/test_inducer_prompts.py
autonomous: true
requirements: [REQ-inducer-prompt-protocol, REQ-inducer-batch-induce]
requirements_addressed: [REQ-inducer-prompt-protocol, REQ-inducer-batch-induce]
must_haves:
  truths:
    - "prompts.py::build_user_message 中 breakout_day OHLC 的相对参考由 '相对盘整起点 close 的 %' 改为 '相对 BO close 的 %'(close 锚点 = 0%);Δ = (price - bo_close) / bo_close * 100。"
    - "inducer_prompts.py::build_batch_user_message 中每条 sample 的 breakout_day_pct 行同步切换到 BO close 锚点;'vs pk_close' 标签替换为 'vs bo_close'。"
    - "consolidation 字段保持不变(5 个无量纲字段语义未变,仅 OHLC 行的 pivot 切换)。"
    - "现有脱敏断言(无 ticker / 无 bo_date / 无绝对价位)继续 PASS;新增断言验证 OHLC 相对值用 BO close 计算。"
  artifacts:
    - path: "BreakoutStrategy/feature_library/prompts.py"
      provides: "build_user_message 用 BO close 作 OHLC pivot"
      contains: "BO close"
    - path: "BreakoutStrategy/feature_library/inducer_prompts.py"
      provides: "build_batch_user_message 用 BO close 作 OHLC pivot"
      contains: "vs bo_close"
    - path: "BreakoutStrategy/feature_library/tests/test_prompts.py"
      provides: "新增/扩展断言:OHLC 用 BO close pivot,close 行显示 +0.00%"
      contains: "bo_close"
    - path: "BreakoutStrategy/feature_library/tests/test_inducer_prompts.py"
      provides: "新增/扩展断言:OHLC 用 BO close pivot,标签是 'vs bo_close' 不是 'vs pk_close'"
      contains: "vs bo_close"
  key_links:
    - from: "prompts.py::build_user_message"
      to: "bo['close'] (作 pivot)"
      via: "bo_close = bo['close'];Δ = (bo['*'] - bo_close) / bo_close * 100"
      pattern: "bo\\[['\"]close['\"]\\]"
    - from: "inducer_prompts.py::build_batch_user_message"
      to: "bo['close']"
      via: "同上"
      pattern: "bo\\[['\"]close['\"]\\]"
---

<objective>
将 `prompts.py::build_user_message` 与 `inducer_prompts.py::build_batch_user_message` 的 breakout_day OHLC pivot 由 `consolidation.pivot_close`(盘整起点 close)切换为 `bo['close']`(突破日 close),实现 D-08:让 GLM-4V 看到的图像 Y 轴(plan 03 的 BO close pivot)与 prompt 文本通道共用同一个零点。

Purpose: 修复"图像与文本两套相对参考系"问题。BO close = 0% 是这批样本的语义不变量(三重锚点重合,跨样本几何同构),GLM-4V cross-attention 时不再撕扯两个不同 pivot。

Output: 两个 prompt 构造函数同步切换,新旧测试 PASS;text 通道不再泄漏 pivot_close 之外的绝对价位。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-librarian-inducer-mvp/01-CONTEXT.md
@docs/research/feature_mining_chart_pivot_decision.md
@CLAUDE.md

<interfaces>
<!-- 当前 prompts.py: 用 consolidation.pivot_close 作 pivot -->
From BreakoutStrategy/feature_library/prompts.py:46-66:
```python
pivot_close = consol.get("pivot_close")
if isinstance(pivot_close, (int, float)) and pivot_close > 0:
    ohlc_line = (
        f"突破日 OHLC(相对盘整起点 close 的 %):"
        f"open={fmt_pct((bo['open'] - pivot_close) / pivot_close * 100)} ..."
    )
else:
    ohlc_line = "突破日 OHLC:N/A(pivot_close 缺失,已跳过绝对价段)\n"
```

<!-- 当前 inducer_prompts.py 同结构 -->
From BreakoutStrategy/feature_library/inducer_prompts.py:81-91:
```python
pivot_close = consol.get("pivot_close")
if isinstance(pivot_close, (int, float)) and pivot_close > 0:
    ohlc_segment = (
        f"breakout_day_pct(vs pk_close): "
        f"open={fmt_pct((bo['open'] - pivot_close) / pivot_close * 100)} ..."
    )
else:
    ohlc_segment = "breakout_day_pct: N/A (pivot_close missing)"
```

<!-- 新方案 D-08 -->
两个函数都改为:
```python
bo_close = bo.get("close")  # 来自 breakout_day["close"](已含在 meta dict 中)
if isinstance(bo_close, (int, float)) and bo_close > 0:
    # close 锚点 = 0%
    open_pct  = (bo["open"]  - bo_close) / bo_close * 100
    high_pct  = (bo["high"]  - bo_close) / bo_close * 100
    low_pct   = (bo["low"]   - bo_close) / bo_close * 100
    close_pct = 0.0   # 定义如此
```

注意:不删除 consol["pivot_close"] 字段 — 它仍是 meta.yaml 的 consolidation anchor 数据,
本 plan 只改 prompt 中 OHLC 的相对参考。consolidation 5 个无量纲字段保持原有 prompt 文本不变。
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: prompts.py 切换 OHLC pivot 到 BO close(D-08)</name>
  <files>BreakoutStrategy/feature_library/prompts.py, BreakoutStrategy/feature_library/tests/test_prompts.py</files>
  <read_first>
    - BreakoutStrategy/feature_library/prompts.py(完整源码)
    - BreakoutStrategy/feature_library/tests/test_prompts.py(完整源码,扩展而非重写)
    - .planning/phases/01-librarian-inducer-mvp/01-CONTEXT.md(D-08 决策原文)
  </read_first>
  <behavior>
    - build_user_message 中:
      - 计算 bo_close = bo.get("close")
      - 若 bo_close 不是正数(罕见,数据异常):降级为 "突破日 OHLC:N/A(bo_close 缺失,已跳过相对价段)"
      - 若 bo_close 正常:输出
        ```
        突破日 OHLC(相对突破日 close 的 %):open=ΔO% high=ΔH% low=ΔL% close=+0.00%
        ```
        其中 Δ = (price - bo_close) / bo_close * 100,close 行显式输出 "+0.00%"。
    - 中文文案"相对盘整起点 close 的 %" → "相对突破日 close 的 %"。
    - 不动 consolidation 字段那段(5 个无量纲字段保持原 prompt 不变)。
    - 不动脱敏断言(ticker / bo_date / 绝对价仍不出现)。
    - 测试新增断言:
      - OHLC 段含 "突破日 close" 字样
      - close 行显示 "+0.00%"(BO close 自身相对自身 = 0%)
      - bo_close = 134 时,open=130 显示约 "-2.99%"(=(130-134)/134*100)
  </behavior>
  <action>
    1. 修改 BreakoutStrategy/feature_library/prompts.py 的 build_user_message:

       ```python
       def build_user_message(meta: dict[str, Any]) -> str:
           bo = meta["breakout_day"]
           consol = meta["consolidation"]
           bo_close = bo.get("close")

           def fmt_pct(v) -> str:
               if not isinstance(v, (int, float)) or isinstance(v, bool):
                   return "N/A"
               return f"{v:+.2f}%"

           def fmt_num(v) -> str:
               if isinstance(v, bool):
                   return "N/A"
               return f"{v:.2f}" if isinstance(v, (int, float)) else "N/A"

           # OHLC → 相对 BO close 的 %(close 锚点 = 0%,与 chart.png Y 轴零点统一)
           if isinstance(bo_close, (int, float)) and bo_close > 0:
               open_pct  = (bo['open']  - bo_close) / bo_close * 100
               high_pct  = (bo['high']  - bo_close) / bo_close * 100
               low_pct   = (bo['low']   - bo_close) / bo_close * 100
               ohlc_line = (
                   f"突破日 OHLC(相对突破日 close 的 %,close 锚点 = 0%):"
                   f"open={fmt_pct(open_pct)} "
                   f"high={fmt_pct(high_pct)} "
                   f"low={fmt_pct(low_pct)} "
                   f"close=+0.00%\n"
               )
           else:
               ohlc_line = "突破日 OHLC:N/A(bo_close 缺失,已跳过相对价段)\n"

           return (
               f"样本类型:股票 K 线突破样本(已脱敏 ticker / 日期 / 绝对价)\n"
               f"{ohlc_line}"
               f"\n盘整阶段量化字段(无量纲):\n"
               f"- 持续时长(bars):{fmt_num(consol['consolidation_length_bars'])}\n"
               f"- 高度百分比:{fmt_num(consol['consolidation_height_pct'])}%\n"
               f"- 距 52 周高点:{fmt_num(consol['consolidation_position_vs_52w_high'])}%\n"
               f"- 量能比(盘整 / 盘整前 60 bars):{fmt_num(consol['consolidation_volume_ratio'])}\n"
               f"- 紧致度(高度 / ATR14):{fmt_num(consol['consolidation_tightness_atr'])}\n"
               f"\n请按 SYSTEM_PROMPT 要求描述这张 K 线图。"
           )
       ```

       同步更新模块顶部 docstring(若有),把 "pk_close" 提及改为 "BO close"。
       函数 docstring 中 "OHLCV 改为相对 consolidation.pivot_close 的百分比" 改为
       "OHLCV 改为相对突破日 close 的百分比(与 chart.png Y 轴 BO close pivot 统一)"。
       并把 "若 meta.consolidation.pivot_close 缺失则跳过 OHLCV 段" 改为
       "若 bo['close'] 缺失或非正则跳过相对价段"。

    2. 在 BreakoutStrategy/feature_library/tests/test_prompts.py 中:

       2a. 既有测试 test_user_message_includes_meta_fields / test_user_message_handles_null_fields /
           test_build_user_message_anonymizes_ticker_date_and_absolute_price /
           test_build_user_message_handles_missing_pivot_close_gracefully:
           保留现有断言(脱敏 + 5 字段保留 + N/A 行为)。
           注意:test_build_user_message_handles_missing_pivot_close_gracefully 中 meta 没有 pivot_close
           但有 breakout_day.close=104.0,新逻辑下会输出 OHLC 相对段(相对 104.0)。
           此时 100.00 / 105.00 / 99.00 / 104.00 不应作为绝对值出现(它们应只以 % 形式出现)。
           现有断言 `assert "100.00" not in msg and "104.00" not in msg` 应继续 PASS,
           因为新逻辑输出 "-3.85%"(=100/104-1)而非 "100.00"。

       2b. 新增 test_build_user_message_uses_bo_close_as_pivot:
           ```python
           def test_build_user_message_uses_bo_close_as_pivot():
               """D-08: OHLC 相对参考改用 bo['close'](= 突破日 close,与 chart.png Y 轴零点一致)。"""
               meta = {
                   "sample_id": "BO_TEST_20240101",
                   "ticker": "TEST",
                   "bo_date": "2024-01-01",
                   "breakout_day": {
                       "open": 130.0, "high": 135.0, "low": 129.0,
                       "close": 134.0, "volume": 1e6,
                   },
                   "consolidation": {
                       "consolidation_length_bars": 30,
                       "consolidation_height_pct": 5.0,
                       "consolidation_position_vs_52w_high": -3.0,
                       "consolidation_volume_ratio": 0.6,
                       "consolidation_tightness_atr": 1.5,
                       "pivot_close": 125.0,  # 仍存在,但 prompt OHLC 不应再用它
                   },
               }
               msg = build_user_message(meta)
               # 新文案应含"突破日 close"作锚点描述
               assert "突破日 close" in msg, f"应注明 BO close 为锚点: {msg!r}"
               # close 行应显示 +0.00%(BO close 相对自身)
               assert "close=+0.00%" in msg, f"close 行应为 +0.00%: {msg!r}"
               # open=130 vs bo_close=134 = -2.99%(不是相对 pivot_close=125 的 +4.00%)
               assert "open=-2.99%" in msg, f"open 应相对 bo_close=134 计算: {msg!r}"
               # 验证不再用 pivot_close=125 (相对 125 的话 open 是 +4.00%)
               assert "+4.00%" not in msg, "OHLC 不应再用 consolidation.pivot_close 作锚点"
           ```

       2c. 新增 test_build_user_message_handles_missing_bo_close_gracefully:
           ```python
           def test_build_user_message_handles_missing_bo_close_gracefully():
               """bo['close'] 缺失或非正时降级为'相对价段 N/A',不泄漏绝对价。"""
               meta = {
                   "sample_id": "BO_TEST_20240101",
                   "ticker": "TEST", "bo_date": "2024-01-01",
                   "breakout_day": {
                       "open": 100.0, "high": 105.0, "low": 99.0,
                       "close": 0.0, "volume": 1e6,   # close = 0,异常数据
                   },
                   "consolidation": {
                       "consolidation_length_bars": 10,
                       "consolidation_height_pct": 3.0,
                       "consolidation_position_vs_52w_high": -5.0,
                       "consolidation_volume_ratio": 0.8,
                       "consolidation_tightness_atr": 2.5,
                       "pivot_close": 95.0,
                   },
               }
               msg = build_user_message(meta)
               # 应降级到 N/A 文本
               assert "N/A" in msg or "bo_close 缺失" in msg
               # 不应泄漏绝对价
               for absolute in ("100.00", "105.00", "99.00"):
                   assert absolute not in msg, f"不应含绝对价 {absolute}"
           ```

    3. 实现完成后人工 grep 验证:
       - `grep "bo\\['close'\\]\\|bo\\[\"close\"\\]" BreakoutStrategy/feature_library/prompts.py` 命中(bo_close 取值)
       - `grep "突破日 close" BreakoutStrategy/feature_library/prompts.py` 命中
       - `grep "pivot_close" BreakoutStrategy/feature_library/prompts.py` 仅出现在 docstring 历史描述或不出现(代码不再依赖它做 OHLC pivot)
  </action>
  <verify>
    <automated>uv run pytest BreakoutStrategy/feature_library/tests/test_prompts.py -v 2>&amp;1 | tail -30</automated>
  </verify>
  <done>
    - build_user_message 用 bo['close'] 作 OHLC pivot
    - close 行恒输出 "+0.00%"
    - 既有脱敏断言全部 PASS
    - 新增 2 个用例 PASS
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: inducer_prompts.py 切换 batch OHLC pivot 到 BO close(D-08)</name>
  <files>BreakoutStrategy/feature_library/inducer_prompts.py, BreakoutStrategy/feature_library/tests/test_inducer_prompts.py</files>
  <read_first>
    - BreakoutStrategy/feature_library/inducer_prompts.py(完整源码)
    - BreakoutStrategy/feature_library/tests/test_inducer_prompts.py(完整源码,扩展)
    - .planning/phases/01-librarian-inducer-mvp/01-CONTEXT.md(D-08)
  </read_first>
  <behavior>
    - build_batch_user_message 中每个 sample 块的 ohlc_segment:
      - 标签由 "breakout_day_pct(vs pk_close)" 改为 "breakout_day_pct(vs bo_close)"
      - 计算用 bo_close = bo.get("close") 而非 consolidation.pivot_close
      - close 行显示 "close=+0.00%"
      - bo_close 缺失/非正时降级 "breakout_day_pct: N/A (bo_close missing)"
    - id_map 行为不变(返回 {"[1]": "BO_AAPL_..."} 等)
    - 现有匿名脱敏断言保持 PASS
  </behavior>
  <action>
    1. 修改 BreakoutStrategy/feature_library/inducer_prompts.py 的 build_batch_user_message 内 for 循环:

       ```python
       for i, meta in enumerate(samples_meta, start=1):
           anon = f"[{i}]"
           id_map[anon] = meta["sample_id"]
           bo = meta["breakout_day"]
           consol = meta["consolidation"]
           bo_close = bo.get("close")
           if isinstance(bo_close, (int, float)) and bo_close > 0:
               open_pct  = (bo['open']  - bo_close) / bo_close * 100
               high_pct  = (bo['high']  - bo_close) / bo_close * 100
               low_pct   = (bo['low']   - bo_close) / bo_close * 100
               ohlc_segment = (
                   f"breakout_day_pct(vs bo_close): "
                   f"open={fmt_pct(open_pct)} "
                   f"high={fmt_pct(high_pct)} "
                   f"low={fmt_pct(low_pct)} "
                   f"close=+0.00%"
               )
           else:
               ohlc_segment = "breakout_day_pct: N/A (bo_close missing)"

           lines.append(
               f"\n{anon}\n"
               f"    {ohlc_segment}\n"
               f"    consolidation: length={fmt_num(consol['consolidation_length_bars'])} bars, "
               f"height={fmt_num(consol['consolidation_height_pct'])}%, "
               f"vs_52w_high={fmt_num(consol['consolidation_position_vs_52w_high'])}%, "
               f"volume_ratio={fmt_num(consol['consolidation_volume_ratio'])}, "
               f"tightness_atr={fmt_num(consol['consolidation_tightness_atr'])}"
           )
       ```

       模块顶部 docstring 中提及的 "OHLC 改为相对 pivot_close 的 %" 改为
       "OHLC 改为相对突破日 close 的 %(与 chart.png Y 轴 BO close pivot 统一)"。

    2. 在 BreakoutStrategy/feature_library/tests/test_inducer_prompts.py 中:

       2a. 既有 test_build_batch_user_message_contains_figure_indices /
           test_build_batch_user_message_handles_null_consolidation_field /
           test_build_batch_user_message_numbers_each_sample /
           test_build_batch_user_message_anonymizes_each_sample /
           test_build_batch_user_message_returns_anonymous_id_mapping:
           - test_build_batch_user_message_anonymizes_each_sample 中:第二个 sample 的
             绝对价位 285/290 在新方案下应仍不出现(被替换为 % 表达式)。该断言应继续 PASS。
             但 sample[0] open=132.5 vs bo_close=134.1 = -1.19%,sample[1] open=285 vs bo_close=289 = -1.38%。
             加一个新断言确保 close 显示 +0.00%(替换不再使用 pivot_close)。

       2b. 新增 test_build_batch_user_message_uses_bo_close_as_pivot:
           ```python
           def test_build_batch_user_message_uses_bo_close_as_pivot():
               """D-08: 每条 sample 的 OHLC 相对参考用 bo['close']。"""
               metas = [
                   {
                       "sample_id": "BO_AAPL_20210617",
                       "ticker": "AAPL", "bo_date": "2021-06-17",
                       "breakout_day": {"open": 130.0, "high": 135.0, "low": 129.0,
                                        "close": 134.0, "volume": 1e6},
                       "consolidation": {
                           "consolidation_length_bars": 30,
                           "consolidation_height_pct": 5.2,
                           "consolidation_position_vs_52w_high": -3.1,
                           "consolidation_volume_ratio": 0.55,
                           "consolidation_tightness_atr": 1.8,
                           "pivot_close": 125.0,   # 不应再被 OHLC 段使用
                       },
                   },
               ]
               msg = build_batch_user_message(metas)
               # 标签改为 vs bo_close
               assert "vs bo_close" in msg, f"标签应为 'vs bo_close',msg={msg!r}"
               assert "vs pk_close" not in msg, "不应再含 'vs pk_close'"
               # close 行恒为 +0.00%
               assert "close=+0.00%" in msg, f"close 行应为 +0.00%: {msg!r}"
               # open=130 vs bo_close=134 ≈ -2.99%
               assert "open=-2.99%" in msg, f"open 应相对 bo_close=134 计算: {msg!r}"
               # 不应使用 pivot_close=125 (相对 125 的话 open=+4.00%)
               assert "+4.00%" not in msg, "OHLC 不应再用 consolidation.pivot_close 作锚点"
           ```

       2c. 新增 test_build_batch_user_message_handles_missing_bo_close_gracefully:
           ```python
           def test_build_batch_user_message_handles_missing_bo_close_gracefully():
               """bo['close'] 缺失或非正时降级为 N/A,不泄漏绝对价。"""
               metas = [{
                   "sample_id": "BO_TEST_20240101",
                   "ticker": "TEST", "bo_date": "2024-01-01",
                   "breakout_day": {"open": 100.0, "high": 105.0, "low": 99.0,
                                    "close": 0.0, "volume": 1e6},  # close=0
                   "consolidation": {
                       "consolidation_length_bars": 5,
                       "consolidation_height_pct": 1.0,
                       "consolidation_position_vs_52w_high": -1.0,
                       "consolidation_volume_ratio": 0.8,
                       "consolidation_tightness_atr": 1.5,
                       "pivot_close": 95.0,
                   },
               }]
               msg = build_batch_user_message(metas)
               assert "bo_close missing" in msg or "N/A" in msg
               for absolute in ("100.00", "105.00", "99.00"):
                   assert absolute not in msg
           ```

    3. 实现完成后人工 grep 验证:
       - `grep "vs bo_close" BreakoutStrategy/feature_library/inducer_prompts.py` 命中
       - `grep "vs pk_close" BreakoutStrategy/feature_library/inducer_prompts.py` 应为空
       - `grep "bo\\['close'\\]\\|bo\\[\"close\"\\]" BreakoutStrategy/feature_library/inducer_prompts.py` 命中
  </action>
  <verify>
    <automated>uv run pytest BreakoutStrategy/feature_library/tests/test_inducer_prompts.py -v 2>&amp;1 | tail -30</automated>
  </verify>
  <done>
    - build_batch_user_message 用 bo['close'] 作每个 sample 的 OHLC pivot
    - 标签 'vs pk_close' 全部替换为 'vs bo_close'
    - 既有 6 个测试 PASS
    - 新增 2 个用例 PASS
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| prompts.py / inducer_prompts.py ↔ GLM-4V API | prompt 文本最终发往 GLM-4V-Flash;但内容由本地 meta dict 构造,无 untrusted 注入 |

## STRIDE Threat Register

无外部 surface — STRIDE 不适用。本 plan 仅修改 prompt 模板的 pivot 计算公式,
输入 meta dict 来自本地 yaml 落盘内容,非 untrusted。
note: prompt 内容不应泄漏 ticker / bo_date / 绝对价(归一化方案 B 现有断言已覆盖)。
mitigations: 沿用现有脱敏断言;新公式 (price - bo_close) / bo_close * 100 不会引入绝对价泄漏。
</threat_model>

<verification>
- 文本通道 OHLC pivot 与图像通道 chart.png Y 轴 pivot 共用 BO close 零点(D-08)
- 现有归一化方案 B 脱敏断言全部 PASS(无 ticker / bo_date / 绝对价)
</verification>

<success_criteria>
- `uv run pytest BreakoutStrategy/feature_library/tests/test_prompts.py BreakoutStrategy/feature_library/tests/test_inducer_prompts.py -v` 100% PASS
- `grep -c "vs pk_close" BreakoutStrategy/feature_library/inducer_prompts.py` 返回 0
- `grep -c "vs bo_close" BreakoutStrategy/feature_library/inducer_prompts.py` ≥ 1
- `grep -c "突破日 close" BreakoutStrategy/feature_library/prompts.py` ≥ 1
- `grep -c "close=+0\\.00%" BreakoutStrategy/feature_library/prompts.py BreakoutStrategy/feature_library/inducer_prompts.py` ≥ 2
</success_criteria>

<output>
After completion, create `.planning/phases/01-librarian-inducer-mvp/01-04-SUMMARY.md`
</output>
