# 原始问题（逐字记录，作为全流程基准锚点）

日期：2026-09-07
上下文：用户在 `path2_apps/bb_v1/dag_spec.py` 中选中了第 54 行
`("distinct_pk",   W.attr("distinct_pk",   ">=", params.burst.distinct_pk_min)),        # ⑤`

## 用户原话

> 你觉得能不能给 dag_spec
> 增加一个打分制度，理由是，利好因素有很多，但是未必同时满足，但是为了权衡普适性和优秀性，必须牺牲一头。就比如 vol_spike
> 很大，但是由于 distinct_pk 不够大，因此被过滤，就很可惜。
> 请你思考分析必要性，可行性，如果可行给出初步方案。
> 创建 agent team 完成分析

## 交付要求（逐条对照）

1. **必要性**：分析引入打分制的必要性
2. **可行性**：分析引入打分制的可行性
3. **初步方案**：若可行，给出初步方案

## 本轮会话的前置事实（供 team 参考，非用户原话）

同一会话稍早修复了 `first_drought` 的窗口左截断问题（`BOEvent.drought_floor`），实测 800 只股票、
2024-01..2026-01 窗口下，「簇首＝扫描窗口内首根 bo」的 burst 共 734 个，逐闸累积通过数为：

| 累积闸 | 通过数 |
|---|---|
| `first_drought >= 20` | 337 |
| ＋ `distinct_pk >= 4` | 74 |
| ＋ `vol_spike >= 8.0` | 1 |
| ＋ `peak_age_max >= 125` | 0 |

这组数字是用户提出打分制的直接触发背景：四个闸的 AND 组合把样本压到 0，而单闸表现强的样本被其他闸拦掉。
