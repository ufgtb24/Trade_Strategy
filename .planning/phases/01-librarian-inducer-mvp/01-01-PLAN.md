---
phase: 01-librarian-inducer-mvp
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - BreakoutStrategy/UI/charts/keyboard_picker.py
  - BreakoutStrategy/UI/charts/tests/test_keyboard_picker.py
autonomous: true
requirements: [REQ-end-to-end-smoke]
requirements_addressed: []
must_haves:
  truths:
    - "Picker 启动时处于 IDLE 状态,无 sticky 提示;按 P 才进入 AWAITING_BO 并显示 sticky '[PICK 1/2] Hover the BO bar, press P'。IDLE 时按 P 无条件进入 AWAITING_BO,不依赖 hover bar 是否存在。"
    - "AWAITING_BO 时按 P,若 hover bar ∈ get_bo_indices() 返回的集合,则进入 AWAITING_LEFT,sticky 切换为 '[PICK 2/2] Hover any K-line BEFORE BO, press P. ESC to cancel'。"
    - "AWAITING_BO 时按 P,若 hover bar ∉ BO 集合,toast 警告 'This bar is not a detected BO. Hover a BO and press P.',状态保持 AWAITING_BO 不变。"
    - "AWAITING_LEFT 时按 P,若 hover idx < bo_idx,触发 on_render(left_idx, bo_idx) 后回到 IDLE 并清 sticky/marker。"
    - "AWAITING_LEFT 时按 P,若 hover idx >= bo_idx,toast 警告 'Pick a K-line BEFORE the BO bar.',状态保持 AWAITING_LEFT 不变。"
    - "Esc 在 AWAITING_BO / AWAITING_LEFT 消费事件(返回 True),清状态并 toast 'Picking cancelled';Esc 在 IDLE 透传(返回 False)。"
    - "同根 K 线: 在 AWAITING_LEFT 状态下若 hover bar == bo_idx,toast 'Same bar, hover another' 并保持当前态。"
    - "D-11: IDLE 状态下 marker 始终为空(由 _reset_silent 在 ESC/完成渲染时保证);IDLE→AWAITING_BO 不调 on_marker_redraw([])(依赖上一轮退出态的不变量)。"
  artifacts:
    - path: "BreakoutStrategy/UI/charts/keyboard_picker.py"
      provides: "三态 KeyboardPicker FSM(IDLE / AWAITING_BO / AWAITING_LEFT) + get_bo_indices 注入钩子"
      contains: "PickerStatus.AWAITING_BO, PickerStatus.AWAITING_LEFT, get_bo_indices"
    - path: "BreakoutStrategy/UI/charts/tests/test_keyboard_picker.py"
      provides: "覆盖三态机所有迁移路径的单元测试"
      contains: "AWAITING_BO, AWAITING_LEFT, get_bo_indices"
  key_links:
    - from: "KeyboardPicker.__init__"
      to: "get_bo_indices: Callable[[], frozenset[int]]"
      via: "构造函数依赖注入"
      pattern: "get_bo_indices"
    - from: "KeyboardPicker.on_press_p (AWAITING_BO)"
      to: "self._get_bo_indices()"
      via: "成员校验集合"
      pattern: "in self._get_bo_indices\\(\\)"
---

<objective>
将 `KeyboardPicker` 由两态(IDLE / PICKING_1)升级为三态(IDLE / AWAITING_BO / AWAITING_LEFT),新增 `get_bo_indices` 注入钩子,并强制顺序约束:第一次 P 必须 hover 在 detected BO 上;第二次 P 必须 hover 在 idx < bo_idx 的 K 线上。错点统一为"stay + toast",不重置不前进。同步重写 `test_keyboard_picker.py` 覆盖所有迁移路径。

Purpose: 修复用户使用 dev UI 后发现的 picker UX 严谨性问题(D-01..D-04)。当前两态机允许"任意两根 K 线"触发渲染,与用户语义"严格第一根 BO + 第二根 BO 之前任意 K 线"不符;同时缺少明确的状态机交互提示,影响样本挑选可靠性。

Output: keyboard_picker.py 升级为三态 FSM + 单元测试全 PASS。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-librarian-inducer-mvp/01-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- 当前 KeyboardPicker 五钩子(本 plan 新增第六钩子 get_bo_indices) -->
现有 __init__ 签名(BreakoutStrategy/UI/charts/keyboard_picker.py:35-49):
```python
def __init__(
    self,
    *,
    get_hovered_bar: Callable[[], Optional[int]],
    on_render: Callable[[int, int], None],
    on_toast: Callable[[str, str], None],
    on_sticky: Callable[[str], None],
    on_marker_redraw: Callable[[Iterable[int]], None],
) -> None:
```

升级后必须新增:
```python
get_bo_indices: Callable[[], frozenset[int]],   # D-02 注入 BO 集合
```

PickerStatus 枚举(本 plan 替换):
- 旧: IDLE / PICKING_1
- 新: IDLE / AWAITING_BO / AWAITING_LEFT

回调签名 on_render(left_idx, right_idx) 维持不变(D-04):新状态机天然保证 right_idx == bo_idx 且 left_idx < bo_idx,sample_picker_handler.handle_endpoints_picked 不需改。
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 重写 KeyboardPicker 为三态 FSM(D-01..D-04)</name>
  <files>BreakoutStrategy/UI/charts/keyboard_picker.py</files>
  <read_first>
    - BreakoutStrategy/UI/charts/keyboard_picker.py(当前两态机完整源码)
    - .planning/phases/01-librarian-inducer-mvp/01-CONTEXT.md(D-01..D-04 决策原文)
    - BreakoutStrategy/UI/charts/canvas_manager.py:1029-1035(_wire_picker 当前传给 __init__ 的 5 个钩子,确认插入新钩子的位置)
  </read_first>
  <behavior>
    <!-- 测试先行(在 Task 2 中写完整 test;此处列出本任务实现必须满足的行为契约) -->
    - PickerStatus 枚举仅含三态: IDLE / AWAITING_BO / AWAITING_LEFT(删除 PICKING_1)
    - __init__ 新增必传 kwarg `get_bo_indices: Callable[[], frozenset[int]]`
    - 初始 status == IDLE,endpoint_bars == []
    - IDLE 按 P(无论 hover 是否 None):无条件进入 AWAITING_BO,调 on_sticky("[PICK 1/2] Hover the BO bar, press P"),不动 marker(暂未选定 BO;D-11 依赖上一轮 _reset_silent 保证 marker 已空)
    - AWAITING_BO 按 P + bar 为 None: toast("info", "Hover over a bar first"),保持 AWAITING_BO
    - AWAITING_BO 按 P + bar ∈ BO 集合: 进入 AWAITING_LEFT,记录 bo_idx,调 on_sticky("[PICK 2/2] Hover any K-line BEFORE BO, press P. ESC to cancel"),调 on_marker_redraw([bo_idx])
    - AWAITING_BO 按 P + bar ∉ BO 集合: toast("warning", "This bar is not a detected BO. Hover a BO and press P."),保持 AWAITING_BO,不动 marker
    - AWAITING_LEFT 按 P + bar 为 None: toast("info", "Hover over a bar first"),保持 AWAITING_LEFT
    - AWAITING_LEFT 按 P + bar < bo_idx: 调 on_render(bar, bo_idx) → 调 _reset_silent()(回 IDLE,清 sticky/marker)
    - AWAITING_LEFT 按 P + bar >= bo_idx: toast("warning", "Pick a K-line BEFORE the BO bar."),保持 AWAITING_LEFT
    - AWAITING_LEFT 按 P + bar == bo_idx(同根): toast("info", "Same bar, hover another"),保持 AWAITING_LEFT
    - Esc 在 AWAITING_BO 或 AWAITING_LEFT: _reset_silent() + toast("info", "Picking cancelled") + 返回 True
    - Esc 在 IDLE: 返回 False(透传)
    - reset() 外部强制清空,不发 toast
  </behavior>
  <action>
    完全重写 BreakoutStrategy/UI/charts/keyboard_picker.py。

    1. 更新模块 docstring:把"端点先后无关(场景 B 通用)"段落替换为:
       "三态 FSM:IDLE → AWAITING_BO → AWAITING_LEFT → IDLE。
        第一次 P 强制 hover 在 detected BO 集合内;第二次 P 强制 hover idx < bo_idx。
        错点保持当前态 + toast 警告,不重置。"

    2. PickerStatus 枚举改为:
       ```python
       class PickerStatus(str, Enum):
           IDLE = "idle"
           AWAITING_BO = "awaiting_bo"
           AWAITING_LEFT = "awaiting_left"
       ```

    3. __init__ 签名加 `get_bo_indices: Callable[[], frozenset[int]]`(放在 get_hovered_bar 之后),
       内部存为 self._get_bo_indices。
       新增成员: self._bo_idx: Optional[int] = None。
       (endpoint_bars 仍保留以兼容 marker_redraw,但语义改为"已选 BO 那一根";IDLE/AWAITING_BO 时为 [],AWAITING_LEFT 时为 [bo_idx])

    4. 重写 on_press_p(self) 为分支 dispatch。
       **关键顺序(WARNING 2 修复):IDLE 分支必须放在 None-check 之前**,因为 D-01 原文要求
       "IDLE 时按 P **无条件**进入 AWAITING_BO,不依赖 hover bar"。在已激活拾取的状态下
       (AWAITING_BO / AWAITING_LEFT)无 hover 才是合理 toast。
       ```python
       def on_press_p(self) -> None:
           # IDLE 分支优先:D-01 要求 IDLE 按 P 无条件进入 AWAITING_BO,
           # 不依赖 hover bar(用户尚未开始挑选,提示其去 hover BO 即可)
           if self.status == PickerStatus.IDLE:
               self.status = PickerStatus.AWAITING_BO
               self._on_sticky("[PICK 1/2] Hover the BO bar, press P")
               # 不调用 on_marker_redraw([]) — marker 已为空(由 _reset_silent 在上一轮 ESC/完成后保证)
               # D-11 要求 IDLE 时 marker 为空,这里依赖上次状态退出时的 _reset_silent 保证不变量
               return

           # 已激活拾取的状态(AWAITING_BO / AWAITING_LEFT)才检查 hover
           bar = self._get_hovered_bar()
           if bar is None:
               self._on_toast("info", "Hover over a bar first")
               return

           if self.status == PickerStatus.AWAITING_BO:
               self._handle_p_in_awaiting_bo(bar)
               return
           if self.status == PickerStatus.AWAITING_LEFT:
               self._handle_p_in_awaiting_left(bar)
               return
       ```

       注意:用户 D-01 把 IDLE→AWAITING_BO 的 sticky 设置写为"按 P 进入 AWAITING_BO"。
       第一次按 P 仅启动状态机(无 hover 也行),不消费 bar;
       第二次按 P 才校验 bar ∈ BO 集合(进入 AWAITING_LEFT 或 toast 留在 AWAITING_BO)。

    5. _handle_p_in_awaiting_bo(self, bar: int):
       - bo_set = self._get_bo_indices()
       - if bar in bo_set:
            self.status = AWAITING_LEFT; self._bo_idx = bar; self.endpoint_bars = [bar]
            self._on_sticky("[PICK 2/2] Hover any K-line BEFORE BO, press P. ESC to cancel")
            self._on_marker_redraw([bar])
       - else:
            self._on_toast("warning", "This bar is not a detected BO. Hover a BO and press P.")
         (不动 status / marker / sticky)

    6. _handle_p_in_awaiting_left(self, bar: int):
       - bo_idx = self._bo_idx(必非 None)
       - if bar == bo_idx:
            self._on_toast("info", "Same bar, hover another"); return
       - if bar >= bo_idx:
            self._on_toast("warning", "Pick a K-line BEFORE the BO bar."); return
       - 否则(bar < bo_idx):
            self._on_render(bar, bo_idx)
            self._reset_silent()

    7. on_press_escape(self) -> bool 维持现有行为(IDLE 返 False;非 IDLE 调 _reset_silent + toast "Picking cancelled" + 返 True)。
       toast 文本由 "Sample picking cancelled" 改为 "Picking cancelled"(与 CONTEXT.md D-01 对齐)。

    8. _reset_silent(self): 清 status=IDLE / endpoint_bars=[] / _bo_idx=None;
       调 on_marker_redraw([]) 和 on_sticky("")。

    9. 注释保持中文(CLAUDE.md 编码规范);sticky / toast 文案保持英文(UI 文本)。

    实现完成后人工 grep 验证:
    - `grep -c "AWAITING_BO\|AWAITING_LEFT" BreakoutStrategy/UI/charts/keyboard_picker.py` ≥ 6
    - `grep -c "get_bo_indices" BreakoutStrategy/UI/charts/keyboard_picker.py` ≥ 2
    - `grep "PICKING_1" BreakoutStrategy/UI/charts/keyboard_picker.py` 应为空(旧枚举已删)
    - `grep -A 3 "if self.status == PickerStatus.IDLE" BreakoutStrategy/UI/charts/keyboard_picker.py` 输出含 `# marker 已为空` 注释(D-11 契约显式化)
  </action>
  <verify>
    <automated>uv run python -c "from BreakoutStrategy.UI.charts.keyboard_picker import KeyboardPicker, PickerStatus; assert {s.value for s in PickerStatus} == {'idle', 'awaiting_bo', 'awaiting_left'}; import inspect; sig = inspect.signature(KeyboardPicker.__init__); assert 'get_bo_indices' in sig.parameters, sig.parameters"</automated>
  </verify>
  <done>
    - PickerStatus 三态枚举正确
    - __init__ 含 get_bo_indices kwarg
    - 旧 PICKING_1 完全移除
    - 模块 docstring 反映新语义
    - IDLE 分支位于 None-check 之前(D-01 无条件进入 AWAITING_BO)
    - IDLE 分支末尾含 `# marker 已为空` 注释(D-11 契约显式化)
  </done>
  <acceptance_criteria>
    - `pytest -k "test_p_in_idle_without_hover_enters_awaiting_bo"` PASS:断言 IDLE + `get_hovered_bar()` 返回 None 时按 P 后 `status == AWAITING_BO` 且 sticky 已 set
    - `grep -A 3 "if self.status == PickerStatus.IDLE" BreakoutStrategy/UI/charts/keyboard_picker.py` 输出含 `# marker 已为空` 注释
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 重写 test_keyboard_picker.py 覆盖三态机迁移</name>
  <files>BreakoutStrategy/UI/charts/tests/test_keyboard_picker.py</files>
  <read_first>
    - BreakoutStrategy/UI/charts/tests/test_keyboard_picker.py(当前两态机测试,完全重写)
    - BreakoutStrategy/UI/charts/keyboard_picker.py(Task 1 完成后的新代码)
    - .planning/phases/01-librarian-inducer-mvp/01-CONTEXT.md(D-01..D-04 行为契约)
  </read_first>
  <behavior>
    用例必须覆盖以下迁移路径:
    - test_initial_state_is_idle(无 sticky/marker)
    - test_p_in_idle_without_hover_enters_awaiting_bo(D-01 无条件进入:`get_hovered_bar()` 返回 None 时按 P,断言 status == AWAITING_BO + sticky 包含 "[PICK 1/2]")
    - test_press_p_in_idle_enters_awaiting_bo_with_sticky_pick_1_of_2(asserting sticky 含 "[PICK 1/2]" 和 "Hover the BO bar")
    - test_press_p_in_awaiting_bo_on_bo_bar_enters_awaiting_left(asserting sticky 含 "[PICK 2/2]" 和 "BEFORE BO";marker_redraw([bo_idx]))
    - test_press_p_in_awaiting_bo_on_non_bo_bar_stays_awaiting_bo_with_warning_toast(asserting toast=("warning", "This bar is not a detected BO. Hover a BO and press P.");status 不变)
    - test_press_p_in_awaiting_bo_without_hover_emits_hover_first_toast(AWAITING_BO + None hover 时按 P,toast=("info", "Hover over a bar first"),status 不变)
    - test_press_p_in_awaiting_left_left_of_bo_triggers_render_and_returns_idle(render=(left, bo);status=IDLE;sticky 末位 "")
    - test_press_p_in_awaiting_left_at_or_right_of_bo_stays_with_warning(asserting toast=("warning", "Pick a K-line BEFORE the BO bar.");status 不变)
    - test_press_p_in_awaiting_left_same_bar_as_bo_emits_same_bar_toast(asserting toast=("info", "Same bar, hover another"))
    - test_escape_in_awaiting_bo_resets_and_consumes(consumed True;status=IDLE;toast=("info","Picking cancelled"))
    - test_escape_in_awaiting_left_resets_and_consumes(同上)
    - test_escape_in_idle_returns_false_for_passthrough
    - test_reset_clears_state_silently(无 toast)
    - 保留并适配既有 test_canvas_manager_escape_priority_routing(签名兼容)
    - 保留并适配 test_canvas_manager_picker_callback_passes_wired_df_not_full_df(注意 _wire_picker 签名将在 plan 02 加 get_bo_indices,本 task 仅准备 test fixture,在 plan 02 实施时再补)
    - 保留并适配 test_canvas_manager_picker_returns_none_when_mouse_never_hovered
  </behavior>
  <action>
    完全重写 BreakoutStrategy/UI/charts/tests/test_keyboard_picker.py。

    1. fixture `picker` 改为接受参数化的 BO 集合:
       ```python
       @pytest.fixture
       def picker_calls():
           return {"render": [], "toast": [], "sticky": [], "marker_redraw": []}

       @pytest.fixture
       def make_picker(picker_calls):
           """工厂:接受可变 hovered_bar 与 bo_set。"""
           state = {"hovered": None}
           def _make(bo_set: frozenset[int] = frozenset({5})):
               return KeyboardPicker(
                   get_hovered_bar=lambda: state["hovered"],
                   get_bo_indices=lambda: bo_set,
                   on_render=lambda l, r: picker_calls["render"].append((l, r)),
                   on_toast=lambda k, t: picker_calls["toast"].append((k, t)),
                   on_sticky=lambda t: picker_calls["sticky"].append(t),
                   on_marker_redraw=lambda b: picker_calls["marker_redraw"].append(list(b)),
               )
           return _make, state
       ```

    2. 删除/替换以下旧测试(语义不再适用):
       - test_press_p_first_time_enters_picking_1
       - test_press_p_second_time_triggers_render
       - test_press_p_order_independent_legal
       - test_press_p_two_arbitrary_bars_now_legal
       - test_press_p_same_bar_twice_no_op(逻辑变了,改为 same_bar_as_bo)

    3. 写满 behavior 列出的所有新用例,每个用例:
       - 显式 set state["hovered"]
       - call on_press_p() 或 on_press_escape()
       - assert picker.status / endpoint_bars / picker_calls

    4. 用例命名严格使用 behavior 中给出的英文 test_ 前缀名,便于 grep 校验覆盖度。
       特别注意 `test_p_in_idle_without_hover_enters_awaiting_bo` 必须存在(WARNING 2 acceptance)。

    5. 既有的两个 canvas_manager 集成回归测试(test_canvas_manager_escape_priority_routing,
       test_canvas_manager_picker_returns_none_when_mouse_never_hovered)保留;
       test_canvas_manager_picker_callback_passes_wired_df_not_full_df 中需要 mock _wire_picker
       的新 get_bo_indices 参数(在 plan 02 中 _wire_picker 签名变更后再校准 — 此处先在测试里
       写 ChartCanvasManager._wire_picker(cm, ax_price=..., df=..., breakouts=..., on_endpoints_picked=fake) 的
       现有调用方式,plan 02 实施时若签名变化由该 plan 同步修)。

    6. 注释中文,断言消息英文。
  </action>
  <verify>
    <automated>uv run pytest BreakoutStrategy/UI/charts/tests/test_keyboard_picker.py -v 2>&amp;1 | tail -30</automated>
  </verify>
  <done>
    - 所有 behavior 列出的 test_* 函数存在并 PASS
    - 旧两态机测试已移除(grep "PICKING_1" 在测试文件中应为空)
    - 三态 FSM 的所有迁移路径有断言覆盖
    - `grep -c "AWAITING_BO\|AWAITING_LEFT" BreakoutStrategy/UI/charts/tests/test_keyboard_picker.py` ≥ 6
    - `test_p_in_idle_without_hover_enters_awaiting_bo` 函数存在并 PASS
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev developer ↔ matplotlib canvas | 仅本地开发者输入,无网络/外部 surface |

## STRIDE Threat Register

无外部 surface — STRIDE 不适用。本 plan 只修改 dev tool FSM 与单元测试,
所有输入(P 键、hover 位置、BO 集合)由开发者本地控制。
mitigations: N/A(无 untrusted input)。
</threat_model>

<verification>
- 三态 FSM 单元测试全部 PASS
- 旧 PICKING_1 残留代码完全移除
- canvas_manager.py 暂未修改(_wire_picker 仍使用 5 钩子);本 plan 修改的 keyboard_picker.py 在 plan 02 落地前可能让 dev UI 实际启动失败 — 这是预期,因为 plan 02 才接线。
</verification>

<success_criteria>
- `uv run pytest BreakoutStrategy/UI/charts/tests/test_keyboard_picker.py -v` 100% PASS
- `grep -c "PICKING_1" BreakoutStrategy/UI/charts/keyboard_picker.py BreakoutStrategy/UI/charts/tests/test_keyboard_picker.py` 返回 0
- `grep -c "get_bo_indices" BreakoutStrategy/UI/charts/keyboard_picker.py` ≥ 2
- D-01..D-04 全部行为可由 grep 测试名 / toast 字符串验证
- IDLE-without-hover 路径(WARNING 2)有专门测试覆盖
- IDLE 分支 `# marker 已为空` 注释存在(WARNING 4 / D-11 契约显式化)
</success_criteria>

<output>
After completion, create `.planning/phases/01-librarian-inducer-mvp/01-01-SUMMARY.md`
</output>
