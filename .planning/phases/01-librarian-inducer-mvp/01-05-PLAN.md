---
phase: 01-librarian-inducer-mvp
plan: 05
type: execute
wave: 3
depends_on: [01-01, 01-02, 01-03, 01-04]
files_modified:
  - BreakoutStrategy/dev/tests/test_sample_picker_handler.py
autonomous: false
requirements: [REQ-test-coverage, REQ-end-to-end-smoke]
requirements_addressed: [REQ-test-coverage, REQ-end-to-end-smoke]
must_haves:
  truths:
    - "全 picker / chart / prompt / handler 测试套件 100% PASS:UI/charts/tests/、feature_library/tests/、dev/tests/ 三个目录。"
    - "test_sample_picker_handler.py 的 anchor 语义注释更新为 'left_idx 兼任 consolidation anchor 与渲染左端' 以反映 D-05 dual-role 决策。"
    - "dev UI 烟测(autonomous: false)确认 P 键三态机交互 + chart.png 渲染窗口收窄 + 字号合适 + Y 轴 BO close pivot 全部按 D-01..D-11 表现。"
  artifacts:
    - path: "BreakoutStrategy/dev/tests/test_sample_picker_handler.py"
      provides: "anchor dual-role 注释更新(代码逻辑不动)"
      contains: "left_idx"
    - path: ".planning/phases/01-librarian-inducer-mvp/01-05-SUMMARY.md"
      provides: "Phase 1 gap-fix 端到端验证记录"
  key_links:
    - from: "全套测试"
      to: "uv run pytest 三目录"
      via: "命令行端到端"
      pattern: "100% passed"
---

<objective>
Phase 1 gap-fix 端到端集成验证:
- 跑通三个测试目录的全部用例(UI/charts/tests/、feature_library/tests/、dev/tests/)
- 更新 test_sample_picker_handler.py 的 anchor 语义注释(代码逻辑不动 — D-04 保证回调签名不变)
- 手工 dev UI 烟测,确认 P 键三态机 + chart.png 渲染窗口收窄 + 字号 + BO close pivot 全部到位

Purpose: 防止 plan 01-04 各自为政导致的链路错位(plan 01 的 FSM、plan 02 的 wire、plan 03 的 renderer、plan 04 的 prompt 在端到端跑通时是否真的协同工作)。

Output: 全套测试 100% PASS;dev UI 实操确认所有 D-01..D-11 落地。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-librarian-inducer-mvp/01-CONTEXT.md
@.planning/phases/01-librarian-inducer-mvp/01-01-PLAN.md
@.planning/phases/01-librarian-inducer-mvp/01-02-PLAN.md
@.planning/phases/01-librarian-inducer-mvp/01-03-PLAN.md
@.planning/phases/01-librarian-inducer-mvp/01-04-PLAN.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 更新 test_sample_picker_handler.py 的 anchor dual-role 注释</name>
  <files>BreakoutStrategy/dev/tests/test_sample_picker_handler.py</files>
  <read_first>
    - BreakoutStrategy/dev/tests/test_sample_picker_handler.py(完整源码)
    - BreakoutStrategy/dev/sample_picker_handler.py(handle_endpoints_picked 的 left_idx → local_pk 链路)
    - .planning/phases/01-librarian-inducer-mvp/01-CONTEXT.md(D-04 + D-05 末段说明 dual-role)
  </read_first>
  <action>
    本 task 不改测试逻辑,仅更新模块顶部 docstring 和断言注释,以反映 plan 03 引入的
    "left_idx 兼任 consolidation anchor + 渲染左端" 双角色语义(D-05 末段的"有意识的耦合保留")。

    1. 在 BreakoutStrategy/dev/tests/test_sample_picker_handler.py 顶部 docstring 加一段:
       ```
       注:plan 03(D-05)起,sample_renderer 的形参 pk_index 重命名为 left_index,
       在 sample_picker_handler 这一层 local_pk 仍是数据语义的 anchor 候选(用户挑的左端
       ≡ consolidation anchor),preprocess_sample 收 pk_index= 时不变;只是渲染层视角下
       它叫 left_index。本测试断言 kwargs["pk_index"] == 50 仍然有效(preprocess_sample
       的 API 形参名未改 — 只有 sample_renderer 的形参名改了)。
       ```

    2. test_handle_endpoints_picked_calls_preprocess_with_local_indices 中
       `assert kwargs["pk_index"] == 50` 那行断言保留不动,但在断言前后加一行注释:
       ```python
       # pk_index 是 preprocess_sample API 形参名(数据层 anchor 语义);
       # 渲染层在 plan 03 改名为 left_index,但本调用边界不影响。
       ```

    3. 不改任何断言或被测函数调用方式。
  </action>
  <verify>
    <automated>uv run pytest BreakoutStrategy/dev/tests/test_sample_picker_handler.py -v 2>&amp;1 | tail -20</automated>
  </verify>
  <done>
    - 注释反映 dual-role 语义
    - 测试 100% PASS(无逻辑改动)
  </done>
</task>

<task type="auto">
  <name>Task 2: 端到端测试套件汇总(自动)</name>
  <files></files>
  <read_first>
    - BreakoutStrategy/UI/charts/tests/test_keyboard_picker.py(plan 01 + plan 02 完成后)
    - BreakoutStrategy/feature_library/tests/test_sample_renderer.py(plan 03 完成后)
    - BreakoutStrategy/feature_library/tests/test_prompts.py(plan 04 完成后)
    - BreakoutStrategy/feature_library/tests/test_inducer_prompts.py(plan 04 完成后)
    - BreakoutStrategy/dev/tests/test_sample_picker_handler.py(本 plan task 1 完成后)
  </read_first>
  <action>
    跑全套相关测试,确保 plan 01-04 落地后无回归:

    ```bash
    uv run pytest \
        BreakoutStrategy/UI/charts/tests/ \
        BreakoutStrategy/feature_library/tests/ \
        BreakoutStrategy/dev/tests/ \
        -v 2>&1 | tail -80
    ```

    若任何测试 FAIL,**不要修复测试断言**,而是定位是哪个 plan 的实现遗漏并回到那个 plan
    补救(如:_wire_picker 漏传 get_bo_indices → 回到 plan 02 task 1;sample_renderer
    残留 axvline → 回到 plan 03 task 1)。

    成功标准:三个目录所有测试 PASS,无 SKIPPED 中包含本次 gap-fix 范围的测试,
    无 ERROR / FAIL。
  </action>
  <verify>
    <automated>uv run pytest BreakoutStrategy/UI/charts/tests/ BreakoutStrategy/feature_library/tests/ BreakoutStrategy/dev/tests/ -v 2>&amp;1 | tail -10 | grep -E "passed|failed"</automated>
  </verify>
  <done>
    - 三个目录所有测试 PASS
    - 无 ERROR
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 手工 dev UI 烟测(确认 D-01..D-11 落地)</name>
  <what-built>
    plan 01-04 已完成:
    - keyboard_picker.py 三态 FSM + get_bo_indices 注入
    - canvas_manager.py _wire_picker 接通新 FSM
    - sample_renderer.py 渲染窗口收窄 + 视觉脱敏 + BO close pivot + 字号压缩
    - prompts.py / inducer_prompts.py 文本通道切到 BO close pivot
  </what-built>
  <how-to-verify>
    在仓库根目录启动 dev UI:

    ```bash
    uv run python scripts/dev_ui.py    # 或当前 dev UI 入口脚本(若路径不同请按实际)
    ```

    选定一支股票后,按以下顺序操作并验证:

    **A. 三态 FSM(D-01..D-04, D-10..D-11)**
    1. 按一次 P:顶部 sticky 应出现 "[PICK 1/2] Hover the BO bar, press P"。
    2. hover 在一根**非** BO 的 K 线上按 P:
       - 应在控制台看到 "[Picker] ⚠️ This bar is not a detected BO. Hover a BO and press P."
       - sticky 仍是 [PICK 1/2](状态保持 AWAITING_BO)。
    3. hover 在一根 BO K 线(图上有 BO 标记的红/绿三角)上按 P:
       - sticky 切换为 "[PICK 2/2] Hover any K-line BEFORE BO, press P. ESC to cancel"
       - 该 BO 那根 K 线上画出**金色实线** axvline 标记。
    4. hover 在 BO **之后**或 == BO 同根的 K 线按 P:
       - 应在控制台看到 "[Picker] ⚠️ Pick a K-line BEFORE the BO bar."(后者)
         或 "[Picker] ℹ️ Same bar, hover another"(同根)
       - sticky 仍是 [PICK 2/2](保持 AWAITING_LEFT)。
    5. hover 在 BO **之前**任意 K 线按 P:
       - 触发 chart.png 渲染流程(后台调 GLM-4V)。
       - sticky 清空,金色 marker 消失,状态回到 IDLE。
    6. 重新进入 AWAITING_BO 后按 ESC:
       - 应看到 "[Picker] ℹ️ Picking cancelled"
       - sticky 清空,marker 清空,回到 IDLE。
    7. IDLE 状态下按 ESC:走原 close-window 路径(无评分窗口时无任何反应,有评分窗口时关闭最后一个)。

    **B. chart.png 渲染(D-05..D-09)**
    选定一个样本完成挑选后,打开 `feature_library/samples/<sample_id>/chart.png` 检查:
    1. 图像只覆盖用户挑选的左端到 BO 的闭区间(K 线根数 ≈ bo_index - left_index + 1),不再是 200 bar 整段。
    2. 图像内**无任何虚线**(无 pk 橙色虚线 / bo 蓝色虚线)。
    3. 图像内**无 legend**。
    4. 标题恒为 "Breakout sample (anonymized)"(无后缀)。
    5. Y 轴 ylabel 为 "Price (% from BO close)";BO bar(图最右一根 K 线)收盘价对应 0% 或 ±0.0%;历史 K 线相对该零点的百分比合理(BO 上涨样本中历史多为负值)。
    6. 字号显著小于过去版本,K 线主体可视面积明显增大。

    **C. prompt 文本通道(D-08,可选验证)**
    若想验证 prompt 文本同步切换,检查 `feature_library/samples/<sample_id>/nl_description.md`
    或在 logging/print 中确认 build_user_message 输出文本含 "突破日 close" / "close=+0.00%"
    字符串(不再含 "相对盘整起点 close")。

    **D. dev UI 全局字号(无关本 plan)**
    canvas_manager.py 内 plt.rcParams.update({"font.size": 32, ...})(165-174 行)是 dev UI
    自身 hover annotation 字号,与 chart.png 无关 — 不在本 plan 验证范围。
  </how-to-verify>
  <resume-signal>
    Type "approved" if all A/B checks pass, or describe specific issues with reference to D-XX numbers (e.g. "D-07 fail: ylabel still says 'vs pk'" or "D-05 fail: chart still shows 200 bars").
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev UI 烟测 | 仅本地交互;chart.png 输出文件仅落本地 feature_library/samples/ |

## STRIDE Threat Register

无外部 surface — STRIDE 不适用。本 plan 仅做端到端测试与人工 UAT,
不引入任何代码 surface。
mitigations: N/A。
</threat_model>

<verification>
- 三目录全测试 PASS
- 手工 UAT 确认 D-01..D-11 全部落地
- 若 UAT 发现 gap,在 SUMMARY.md 中记录并触发对应 plan 修订
</verification>

<success_criteria>
- `uv run pytest BreakoutStrategy/UI/charts/tests/ BreakoutStrategy/feature_library/tests/ BreakoutStrategy/dev/tests/ -v` 全 PASS
- 手工 UAT checkpoint 用户回答 "approved"
- D-01..D-11 全部可在 dev UI 中验证到位
</success_criteria>

<output>
After completion, create `.planning/phases/01-librarian-inducer-mvp/01-05-SUMMARY.md` 记录:
- 全套测试 PASS 数量
- 手工 UAT 反馈
- 任何残留 gap(若有,标注是否需要新一轮 plan-phase --gaps)
</output>
