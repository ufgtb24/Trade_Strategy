breakthrough_detector:
  cache_dir: ./cache
  exceed_threshold: 0.004
  min_relative_height: 0.3
  min_side_bars: 5
  peak_supersede_threshold: 0.03
  total_window: 20
  use_cache: false
feature_calculator:
  continuity_lookback: 5
  stability_lookforward: 10
quality_scorer:
  breakthrough_weights:
    change: 0.05
    continuity: 0.1
    gap: 0.0
    momentum: 0.2
    resistance: 0.50  # 合并原 resistance(0.3) + historical(0.25)，略降
    stability: 0.0
    volume: 0.15
  peak_weights:
    candle: 0.4
    volume: 0.6
  # 阻力重要性参数（新架构）
  resistance_importance:
    cluster_density_threshold: 0.03  # 簇分组阈值
    age_base_days: 21                # 年龄基准（1个月）
    age_saturation_days: 504         # 年龄饱和（2年）
