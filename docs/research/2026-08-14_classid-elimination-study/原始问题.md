# 原始问题（2026-08-14，agent team 研究基准锚点）

## 本轮原始问题（逐字）

> 再进行第二轮思考，看看该方案是否最佳，有没有更好的方案来达到彻底废弃 class_id，并且不影响功能的目的?
> 从第一性原理来看，废弃 class_id 所要承受的代价是否值得(不考虑开发成本)？
> 从多维度的评估，比如说易于扩展，易于维护，易于 skill 增加新的 detector 等等
> 这次启用 agent team 研究

（"该方案"指 lead 已提出的方案 A，全文见 context.md §3。）

## 关联历史决策（同样为用户原话，供锚定）

1. 用户观察入口A结果显示类名后（2026-08-14）：
> 当前的 web ui 的 DetailSidebar 中，对入口A结果，显示的是类名，我觉得应该显示 node_id，你觉得呢？因为这样才和整个前端的显示风格一致。前端应该是看不到代码层面的数据结构的。可以参考所有重构执行之前的行为(分支 best_ever_v1)

2. 用户确认设计意图（2026-08-14）：
> 最新的设计中，整个 repo 应该不再有 class_id 这个概念了对吧

3. 用户拍板执行方向（2026-08-14）：
> 做吧，把 class_id 彻底清掉统一到 node_id

4. 用户否决反向路线并提高目标（2026-08-14，/subtask 派发时）：
> 我已经叫停了探索，我觉得有必要彻底消除 class_id, 即使另一个 session 给出了调试中需要 class_id，但是我需要你想办法解决，实在不行再用 class_id
