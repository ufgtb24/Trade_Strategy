# 原始问题（用户原话，逐字记录）

## 本轮任务（2026-08-31）

> 我已经将代码 reset --hard 到执行 plan 之前。
> 那么现在有三种方法需要你思考：
> - 方案 B，bo detector消费 pk stream，由于是pk，bo 的计算过程互不可见，因此无法复刻现有版本中 pk 和 bo 的动态交互。
> - 上述递归 reference。但是会漏掉一些 peak，无法彻底满足需求。
> - 打破"铁律：一个 detector 只产生一个 stream". 我认为打破铁律可以视为拓展框架功能的机会，而且有现实应用场景。不需要兼容已有 apps，因为这些 apps 并不完善，且重新扫描的成本很低。需要兼容的是其他单一 stream 的 detector，比如 on_gate 指定目标 stream，保证框架的协议统一性。
>
> 请你评估几种方法，并给出推荐方法的实现方案。
> 开启新的 agent team

## 前序诉求（同一条线索上，逐字）

需求的最初表述：

> 之前的研究 docs/research/2026-08-31_pk-as-event-and-multi-measure 中，我自己觉得 Q2 已经不再需要，因为我将 peak_measure 理解为一个参数，那么其实只是一种选择，既然是选择，就有取舍，就像其他的任何参数一样，没必要特殊对待。但是对于 Q1，我仍然想控制其显示，使得K线图支持显示被其他 pk 吃掉，未被突破的 pk (包括这次新加的大阴线)，但是我希望确认该功能不会破坏引擎和 web ui 的通用性，如果要破坏通用性而专门为 pk 开发一套功能，那么就不值得。最后，加上上述的 kind 方案，纳入 bo_detector 的重构。

对递归 reference 的提出：

> 既然我的最核心诉求是显示被吞掉的 pk，那么回到最初的 bo detector，用第一性原理告诉我，为啥不能用递归的 reference 来显示，现有代码中显示 pk 是作为 bo 的 reference，那么被吞掉的 pk 也可以作为吞掉它的 pk 的 reference。对么

对方案 B 的倾向（在被告知 B′ 有参数一致性风险后）：

> 而方案 B 我觉得更符合引擎的设计，不需要冗余计算，不需要保证一致性。而且我认为当前的 apps 都还不完善，没有不能放弃的理由，我完全可以接受重新创建语义等价的新 app，所以不要顾忌这部分改造成本。
> 我希望的是从第一性原理出发最优雅的方案

对铁律的质疑：

> 甚至，引擎的铁律 "一个 detector 只能出一条流" 是否有什么坚持的必要，还是说只是之前没有遇到需要产生多个 streams 的场景？产生多个 streams 会对引擎有什么伤害？如果没有伤害，能不能取消所谓的铁律，并扩展引擎的功能

对时序问题的反驳（针对峰域/突破域分离方案）：

> 但是将 peak 和 bo 完全解耦合会有一个时序问题：假如在 peak 域， pk0 被 pk1 取代，那么最终 peak_detector 输出没有 pk0. 但是原本在一起计算 pk,bo 时，pk0 可能先被一个 bo0 突破(甚至 pk1 本身就是 bo0)。那么原本混合计算得到的 bo0 在独立计算时不存在了。

## 已在 brainstorm 中拍板、本轮不重新讨论的决策

- 显示范围：**全部 pk 都画**，三态（存活未突破 / 被突破 / 被其他 pk 吃掉）可视区分
- `bo_only` 也显示 pk，与其它 pattern 统一显示规则
- 大阴线 kind 默认开启，`bear_drop=0.05` / `bear_min_rh=0.20`
- marker 视觉编码方案 A：填充度编码状态 + ▽ 底横线编码 kind（使用者色盲，不得依赖色相）
- 多 `pk_measure` 并行：**判死**，不再讨论（peak_measure 视作普通参数）

## 硬约束

- 不为 pk 开发专用渲染路径；渲染层改动必须类型无关
- 框架协议要统一：若扩展引擎，单 stream detector 必须继续正常工作
