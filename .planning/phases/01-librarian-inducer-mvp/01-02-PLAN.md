---
phase: 01-librarian-inducer-mvp
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - BreakoutStrategy/UI/charts/canvas_manager.py
autonomous: true
requirements: [REQ-end-to-end-smoke]
requirements_addressed: []
must_haves:
  truths:
    - "canvas_manager._wire_picker 注入 get_bo_indices 闭包,数据源是当前 update_chart 收到的 breakouts 列表的 .index 字段。"
    - "_wire_picker 形参 on_endpoints_picked 类型注解与运行时 4-arg 调用(left, right, df, breakouts)一致,用 forward reference 避免 import 环依赖。"
    - "进入 AWAITING_LEFT 时 dev UI 上画一根金色 axvline marker(BO 那根),回到 IDLE 时 marker 清除。"
    - "切换 ticker / 重新 update_chart 时 picker 实例被重建,旧状态不残留。"
  artifacts:
    - path: "BreakoutStrategy/UI/charts/canvas_manager.py"
      provides: "_wire_picker 接通新三态 FSM + get_bo_indices 闭包 + 类型注解修正"
      contains: "get_bo_indices"
  key_links:
    - from: "ChartCanvasManager._wire_picker"
      to: "KeyboardPicker(get_bo_indices=...)"
      via: "闭包从 update_chart 传入的 breakouts 列表读 .index"
      pattern: "frozenset.*breakouts.*index"
    - from: "ChartCanvasManager.update_chart"
      to: "self._wire_picker(..., breakouts=breakouts, ...)"
      via: "现有路径,无需改"
      pattern: "_wire_picker"
---

<objective>
将 `ChartCanvasManager._wire_picker` 接通 plan 01 升级后的三态 KeyboardPicker:新增 `get_bo_indices` 闭包,从 `update_chart` 收到的 `breakouts: list[Breakout]` 读取 `.index` 字段构造 `frozenset[int]`,作为 BO 集合校验源。同时修正 `_wire_picker` 形参 `on_endpoints_picked` 的类型注解(运行时实际是 4-arg 调用,既有 type hint 漏洞)。验证 marker_redraw 行为(进入 AWAITING_LEFT 画 [bo_idx],回到 IDLE 清空)与现有 `_redraw_endpoint_markers` 实现兼容。

Purpose: 落地 D-10..D-11(BO 集合暴露 + marker 行为微调),让 dev UI 实际能消费新 FSM。本 plan 不改 `_show_picker_toast` / `_set_picker_sticky` / `_redraw_endpoint_markers` 的实现 — 它们已就位。

Output: canvas_manager.py 的 _wire_picker 通过新签名接线,dev UI 启动后 P 键交互按 D-01..D-04 + D-10..D-11 表现。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-librarian-inducer-mvp/01-CONTEXT.md
@.planning/phases/01-librarian-inducer-mvp/01-01-PLAN.md
@CLAUDE.md

<interfaces>
<!-- Breakout dataclass 字段名 -->
From BreakoutStrategy/analysis/breakout_detector.py:111-121:
```python
@dataclass
class Breakout:
    index: int   # 突破日在价格序列中的索引(NOT `idx`,是 `index`)
    # ...其他字段
```

<!-- canvas_manager 当前 _wire_picker 签名(本 plan 修改) -->
From BreakoutStrategy/UI/charts/canvas_manager.py:993-1035:
```python
def _wire_picker(self, ax_price, df, breakouts: list,
                 on_endpoints_picked: Optional[Callable[[int, int], None]]) -> None:
    # 内部构造 KeyboardPicker 实例,传入 5 个钩子
```

注:`on_endpoints_picked` 当前类型注解 `Callable[[int, int], None]` 是错的 —
运行时实际由 `sample_picker_handler.handle_endpoints_picked(left, right, df, breakouts)`
传入,签名是 4 arg。本 plan 修正为 `Callable[[int, int, 'pd.DataFrame', list], None]`。

<!-- 升级后必须传 6 个钩子(plan 01 已实现) -->
KeyboardPicker.__init__ 现在要求:
```python
KeyboardPicker(
    get_hovered_bar=...,
    get_bo_indices=...,      # 新增 — 本 plan 提供
    on_render=...,
    on_toast=...,
    on_sticky=...,
    on_marker_redraw=...,
)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: _wire_picker 注入 get_bo_indices 闭包(D-10)+ 修正 on_endpoints_picked 类型注解</name>
  <files>BreakoutStrategy/UI/charts/canvas_manager.py</files>
  <read_first>
    - BreakoutStrategy/UI/charts/canvas_manager.py:993-1050(_wire_picker / _redraw_endpoint_markers 当前实现)
    - BreakoutStrategy/UI/charts/canvas_manager.py:381-386(update_chart 中调 _wire_picker 的位置)
    - BreakoutStrategy/UI/charts/keyboard_picker.py(plan 01 升级后 __init__ 签名)
    - BreakoutStrategy/analysis/breakout_detector.py:111-130(Breakout dataclass,确认字段名是 .index)
    - BreakoutStrategy/dev/sample_picker_handler.py(handle_endpoints_picked 4-arg 实际签名,确认类型注解修正后与运行时一致)
    - .planning/phases/01-librarian-inducer-mvp/01-CONTEXT.md(D-10/D-11 决策)
  </read_first>
  <behavior>
    - _wire_picker 内部新建 get_bo_indices 闭包: `lambda: frozenset(b.index for b in breakouts)`(注意是 b.index,不是 b.idx)
    - 把 get_bo_indices 作为新 kwarg 传入 KeyboardPicker(...)
    - update_chart 调用 _wire_picker 的位置不变(已经传 breakouts=breakouts)
    - on_endpoints_picked 为 None 时仍走 self._picker = None 的早返路径(不构造 picker)
    - **类型注解修正(WARNING 5)**:`_wire_picker` 形参 `on_endpoints_picked` 类型注解从
      `Callable[[int, int], None]` 改为 `Callable[[int, int, 'pd.DataFrame', list], None]`
      (使用 forward reference `'pd.DataFrame'` 避免 runtime import 加载;`list` 表示
      `list[Breakout]`,不强行 import Breakout 以避免循环依赖)
    - _redraw_endpoint_markers 实现保持不变 — 现有逻辑已支持任意 Iterable[int],FSM 进入 AWAITING_LEFT 时传 [bo_idx],回 IDLE 传 [],兼容。
  </behavior>
  <action>
    在 BreakoutStrategy/UI/charts/canvas_manager.py 中:

    1. 修改 `_wire_picker` 方法(当前位于约 993-1035 行)。
       在构造 KeyboardPicker 之前加 BO 集合闭包:
       ```python
       def get_bo_indices() -> frozenset[int]:
           # 从 update_chart 时传入的 breakouts 列表读 .index 字段构造集合
           # (Breakout dataclass 字段是 .index,不是 .idx — 见 breakout_detector.py:121)
           return frozenset(b.index for b in breakouts)
       ```

    2. **修正 `_wire_picker` 形参类型注解(WARNING 5)**:
       把 `on_endpoints_picked` 的类型注解从:
       ```python
       on_endpoints_picked: Optional[Callable[[int, int], None]]
       ```
       改为:
       ```python
       on_endpoints_picked: Optional[Callable[[int, int, 'pd.DataFrame', list], None]]
       ```
       说明:
       - 运行时实际调用方 `sample_picker_handler.handle_endpoints_picked(left, right, df, breakouts)`
         是 4-arg 调用,旧的 2-arg 类型注解是漏洞。
       - `'pd.DataFrame'` 用 forward reference(单引号字符串字面量),
         不需要在 canvas_manager.py 顶部新增 `import pandas as pd`(若已有 import 则保持原样)。
       - `list` 表示 `list[Breakout]`,不强行 import Breakout 类型避免潜在循环依赖。
       - 若文件顶部 `from typing import Callable, Optional` 已存在,无需新增 import。

    3. 把 KeyboardPicker(...) 调用改为:
       ```python
       self._picker = KeyboardPicker(
           get_hovered_bar=get_hovered_bar,
           get_bo_indices=get_bo_indices,   # D-10 新增
           on_render=on_render,
           on_toast=on_toast,
           on_sticky=on_sticky,
           on_marker_redraw=on_marker_redraw,
       )
       ```

    4. _redraw_endpoint_markers 维持不变(已正确处理 [] 与 [bar] 两种入参 — 见 1037-1050 行)。
       D-11 的语义"AWAITING_LEFT 时画 [bo_idx],IDLE 时画 []"由 plan 01 的 FSM 已实现(_handle_p_in_awaiting_bo 时 on_marker_redraw([bar]),_reset_silent 时 on_marker_redraw([]))。

    5. 不改 update_chart 的调用方式(已经传 breakouts=breakouts)。

    6. 不改 _show_picker_toast / _set_picker_sticky 的实现(它们已支持新 sticky/toast 字符串)。

    7. update_chart 顶部那段 plt.rcParams.update(...) 与本 plan 无关(那是 dev UI 全局字号,不是 chart.png),保持原样。

    实现完成后人工 grep 验证:
    - `grep -c "get_bo_indices" BreakoutStrategy/UI/charts/canvas_manager.py` ≥ 2(一次定义,一次传参)
    - `grep "frozenset(b.index" BreakoutStrategy/UI/charts/canvas_manager.py` 命中
    - `grep "b.idx" BreakoutStrategy/UI/charts/canvas_manager.py` 应为空(避免错用字段名)
    - `grep "Callable\[\[int, int, .pd.DataFrame., list\]" BreakoutStrategy/UI/charts/canvas_manager.py` 输出非空(WARNING 5 acceptance)
  </action>
  <verify>
    <automated>uv run python -c "from BreakoutStrategy.UI.charts.canvas_manager import ChartCanvasManager; import inspect; src = inspect.getsource(ChartCanvasManager._wire_picker); assert 'get_bo_indices' in src, 'get_bo_indices not wired'; assert 'b.index for b in breakouts' in src, 'BO indices closure must use .index field'; assert \"Callable[[int, int, 'pd.DataFrame', list], None]\" in src or 'Callable[[int, int, \\'pd.DataFrame\\', list], None]' in src, 'on_endpoints_picked type hint must be 4-arg with forward ref pd.DataFrame'"</automated>
  </verify>
  <done>
    - _wire_picker 把 get_bo_indices 闭包注入新 picker
    - 闭包用 b.index(不是 b.idx)
    - on_endpoints_picked 类型注解修正为 4-arg + forward reference
    - on_endpoints_picked is None 时仍早返
    - 现有 _redraw_endpoint_markers 不动
  </done>
  <acceptance_criteria>
    - `grep "Callable\[\[int, int, .pd.DataFrame., list\]" BreakoutStrategy/UI/charts/canvas_manager.py` 输出非空(WARNING 5)
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 同步 test_keyboard_picker.py 的 canvas_manager 集成回归用例</name>
  <files>BreakoutStrategy/UI/charts/tests/test_keyboard_picker.py</files>
  <read_first>
    - BreakoutStrategy/UI/charts/tests/test_keyboard_picker.py(plan 01 重写后版本)
    - BreakoutStrategy/UI/charts/canvas_manager.py(plan 02 task 1 修改后版本)
  </read_first>
  <behavior>
    - 既有 test_canvas_manager_picker_callback_passes_wired_df_not_full_df 中 _wire_picker 调用方式不变(签名未变),但内部 KeyboardPicker 构造现在多了 get_bo_indices kwarg — 该测试通过 cm._picker = real_picker 间接依赖,需断言 real_picker._get_bo_indices() 返回的集合包含 mock breakout 的 index。
    - 新增 test_wire_picker_get_bo_indices_returns_frozenset_of_breakout_indices:断言 _wire_picker 内部构造的 picker 上调 _get_bo_indices() 返回 `frozenset({5})`(基于 mock breakouts=[BO(index=5)])。
  </behavior>
  <action>
    在 BreakoutStrategy/UI/charts/tests/test_keyboard_picker.py 末尾追加:

    ```python
    def test_wire_picker_injects_bo_indices_closure_from_breakouts():
        """_wire_picker 注入的 get_bo_indices 闭包应返回 frozenset(b.index for b in breakouts)。"""
        from unittest.mock import MagicMock
        from BreakoutStrategy.UI.charts.canvas_manager import ChartCanvasManager

        cm = MagicMock(spec=ChartCanvasManager)
        cm._endpoint_marker_lines = []
        cm._last_hover_x = 0
        cm._hover_in_chart = False
        cm.canvas = MagicMock()
        cm.fig = MagicMock()
        cm.fig.axes = [MagicMock()]

        bo1 = MagicMock(); bo1.index = 5
        bo2 = MagicMock(); bo2.index = 10
        breakouts = [bo1, bo2]

        ChartCanvasManager._wire_picker(
            cm, ax_price=MagicMock(), df=MagicMock(),
            breakouts=breakouts,
            on_endpoints_picked=lambda l, r, df_, bo_: None,
        )
        real_picker = cm._picker
        assert real_picker is not None
        # 闭包应返回 frozenset({5, 10})
        assert real_picker._get_bo_indices() == frozenset({5, 10})
    ```

    并 update test_canvas_manager_picker_callback_passes_wired_df_not_full_df 使其继续 PASS — 该测试调 _wire_picker 后取 cm._picker,需要保证 plan 01 的 KeyboardPicker.__init__ 在缺少 get_bo_indices 时会 raise(确保签名正确性);若现在用 mock breakouts=[bo_mock] 调用 _wire_picker,闭包会读 bo_mock.index(=5),正确。

    无需改其他测试。
  </action>
  <verify>
    <automated>uv run pytest BreakoutStrategy/UI/charts/tests/test_keyboard_picker.py -v 2>&amp;1 | tail -30</automated>
  </verify>
  <done>
    - 新增 test_wire_picker_injects_bo_indices_closure_from_breakouts PASS
    - 既有 canvas_manager 集成回归用例 PASS
    - 整个 test_keyboard_picker.py 100% PASS
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev developer ↔ matplotlib canvas | 仅本地开发者输入,无网络/外部 surface |

## STRIDE Threat Register

无外部 surface — STRIDE 不适用。本 plan 仅在 dev UI 集成层连线 picker,
breakouts 列表来自本地 BreakoutDetector 计算结果,非 untrusted input。
mitigations: N/A。
</threat_model>

<verification>
- _wire_picker 注入闭包正确,字段名严格用 b.index
- _wire_picker 形参 on_endpoints_picked 类型注解修正为 4-arg + forward reference
- dev UI 启动后(手工 UAT 在 plan 05 验证)P 键按 D-01..D-04 + D-10..D-11 表现
</verification>

<success_criteria>
- `uv run pytest BreakoutStrategy/UI/charts/tests/test_keyboard_picker.py -v` 100% PASS
- `grep -c "get_bo_indices" BreakoutStrategy/UI/charts/canvas_manager.py` ≥ 2
- `grep "b\.idx[^_]" BreakoutStrategy/UI/charts/canvas_manager.py` 应为空(无错误字段引用)
- `grep "Callable\[\[int, int, .pd.DataFrame., list\]" BreakoutStrategy/UI/charts/canvas_manager.py` 输出非空(WARNING 5)
</success_criteria>

<output>
After completion, create `.planning/phases/01-librarian-inducer-mvp/01-02-SUMMARY.md`
</output>
