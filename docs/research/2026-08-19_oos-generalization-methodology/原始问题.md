# 原始问题（2026-08-19，逐字记录）

我只跑了一次，为了防止 2026H1 污染，是不是现在立刻回到 [2024-1-1,2026-1-1] 去调参就可以避免。之前是在 [2025-1-1,2026-1-1] 调参，我向前增加一年，增加样本量，并且找到 match 数尽量多的 pattern 防止过拟合。并且不再轻易使用 2026-1-1 之后的数据，而是在  [2024-1-1,2026-1-1] 尽量做到指标够好的同时， match 够多(这是之前忽视的)， match 够多才能避免过拟合。
之前使用 bs 作为主要策略框架时，采用了 optuna 优化，然后外推成绩就很差，因为 optuna 太容易过拟合，这次使用 path2，我就避免使用 optuna 等优化工具调参，防止过拟合，但是外推也不理想。
因此我认为唯一能在不污染外推数据(基于 2026-1-1 之后的反馈调参)的情况下增加外推成绩的最好办法就是在训练数据中得到尽量大的 match 数。除此之外，基于量化交易的知识，你有没有什么建议，让我能够在不数据泄露的情况下，增加外推成功率。2026-1-1 之后的数据我不算频繁测试，有什么关于量化交易的经验和原则，能增加"在训练集中开发完，外推时一击即中"的概论。
为了回答上述问题，如有必要:1. 上网查看社区经验。2.使用多个子代理
