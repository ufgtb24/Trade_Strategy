# path2 数据层刷新步骤(web-loop refresh agent 执行)

> 所有命令在 refresh agent 启动时的 cwd(=项目根 / 当前 worktree)下执行,不使用绝对路径——保证同一份说明文件在任意 worktree 通用。

1. 重启后端:`lsof -ti:8000 | xargs -r kill`(按端口精确,**勿 pkill -f**)→ `nohup uv run python -m path2_web.main > /tmp/path2_web_backend.log 2>&1 &` → `curl -sf http://localhost:8000/patterns` 轮询至 200。
2. 触发重扫:`POST http://localhost:8000/scan` body `{"pattern_id":"bottom_breakout_burst","label_horizon":20}`(迭代压墙钟可加 `"ticker_regex":"^(ENR|IONQ|VIAV)$"` 等已知命中子集(2025 窗全集命中:ENR/GORO/IONQ/LPSN/MNPR/MSN/RNXT/SOHOO/VIAV/VRME))→ 返回 `{"scan_id":scan_ts}`。
3. poll 结果:`GET http://localhost:8000/scans/bottom_breakout_burst/<scan_ts>` 至 200 且 results 非空(扫描进行中返回 404,间隔 10s retry 6-12 次,不用 SSE;全集 6048 只约 1-2 分钟)。
4. 前端 reload。
